-- Scope E: No new RLS policies needed for the added columns.
-- Existing org-scoped SELECT/UPDATE policies on AutomationSuggestion cover the new fields.
-- This migration is a no-op placeholder to keep migration numbering consistent.
SELECT 1;
