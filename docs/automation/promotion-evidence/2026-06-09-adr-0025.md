# ADR-0025 Promotion Evidence — 2026-06-09

This file is the durable audit anchor for the ADR-0025 promotion. It pairs (a) the SHA of the migration file at apply time, (b) the SHA of the verification script, (c) the SHA of this runbook, (d) the project ref of the target environment, and (e) the 12-query gate output. A future auditor can re-run the verification script against the same project (or a clone) and confirm the 12 queries still pass.

## Target environment

- **Date:** 2026-06-09
- **Owner:** cheikh.witm@proton.me (the only role authorized to type `npx prisma migrate deploy`)
- **Orchestrator (evidence-recorder):** Mavis
- **Supabase project ref:** `czinchfegtglmrloxlmh`
- **Project URL:** `https://czinchfegtglmrloxlmh.supabase.co` (live, returns 404 on root as expected for Supabase project domains)
- **Postgres direct host:** `db.czinchfegtglmrloxlmh.supabase.co:5432`
- **Postgres pooler host (used by the verification script):** `aws-0-eu-west-1.pooler.supabase.com:6543`
- **Database role:** `postgres` (the only role that can read `pg_catalog`, `pg_policies`, `pg_trigger`, `pg_proc`, `pg_roles`)
- **Password:** redacted (lives in the owner's terminal session only, per AGENTS.md)

## Migration SHAs at apply time

| Migration | Filename | Git commit that introduced the migration | SHA-256 of the migration file at apply time |
|---|---|---|---|
| `20260608175159_automation_handover_draft_policies` | `prisma/migrations/20260608175159_automation_handover_draft_policies/migration.sql` | `d4e6570` (HEAD on `phase-b-multistandort` at promotion time) | `b438f400c0d9045fb66aa16ff488026eb2b54af1c6b46158667ecaff89c39202` |

## Verification script SHA

- **Filename:** `scripts/verify-adr-0025-handover-draft-policies.ts`
- **Git commit that introduced the script:** `d4e6570` (HEAD on `phase-b-multistandort` at promotion time)
- **SHA-256 of the script at apply time:** `ac438ca1dce0f5198ad3c43114b1f03fb3cdcd98c47ab99e355b684279385315`

## Runbook SHA

- **Filename:** `docs/automation/promotion-evidence/RUNBOOK-adr-0025.md`
- **Git commit that introduced the runbook:** `d4e6570` (HEAD on `phase-b-multistandort` at promotion time)
- **SHA-256 of the runbook at apply time:** `847f318d9efb96f31f48ad43923643a9ace3a8a5f6ab42cb6374f469436cfc56`

## Pre-apply state

From the owner's `npx prisma migrate status` output before the apply:

```
31 migrations found in prisma/migrations

1 not yet applied:
  20260608175159_automation_handover_draft_policies
```

## Apply step

Owner-typed command:

```bash
npx prisma migrate deploy
```

Output (owner-supplied):

```
1 migration applied:
  20260608175159_automation_handover_draft_policies

All migrations have been successfully applied.
Database schema is up to date!
```

## Post-apply state

From the owner's `npx prisma migrate status` output after the apply:

```
32 migrations found in prisma/migrations

Database schema is up to date!
```

## 12-query gate output

From the owner's `npx tsx scripts/verify-adr-0025-handover-draft-policies.ts` output:

```
ID  Description                                                            Expected         Actual
--  --------------------------------------------------------------------- --------------- ----------------------------------------
 1  Migration 20260608175159_automation_handover_draft_policies is in _pr 1               PASS: 1
 2  shift_handover_draft_lead_or_manager_insert policy exists on ShiftHan 1               PASS: 1
 3  shift_handover_draft_lead_or_manager_update policy exists on ShiftHan 1               PASS: 1
 4  Insert policy is FOR INSERT TO authenticated (not TO app_runtime)     {authenticated} PASS: {authenticated} / INSERT
 5  Update policy is FOR UPDATE TO authenticated                          {authenticated} PASS: {authenticated} / UPDATE
 6  authenticated has INSERT+UPDATE on ShiftHandoverDraft (2 grants)      2               PASS: 2
 7  app_runtime has INSERT+UPDATE on ShiftHandoverDraft (2 grants)        2               PASS: 2
 8  B-1/B-2 baseline regression: 5 tables / 7 enums / 5 SELECT / 2 trigge 5/7/5/2         PASS: 5/7/5/2
 9  AutomationDecision BEFORE UPDATE / BEFORE DELETE triggers still prese 2               PASS: 2
10  app_runtime role exists with NOBYPASSRLS (per ADR-0017)               rolbypassrls =  PASS: rolbypassrls = false
11  Existing 4 inventory tables are untouched (no regression)             4               PASS: 4
12  Migration's DO $$ regression guard would pass (4/4 assertions)        1/1/1/1         PASS: 1/1/1/1
Summary: 12/12 passed, 0/12 failed.
```

## 12-query results table

One row per check; filled at apply time by the orchestrator from the 12-query gate output above.

| # | Check | Expected | Actual | Pass |
|---|---|---|---|---|
| 1 | Migration `20260608175159_automation_handover_draft_policies` is in `_prisma_migrations` | 1 | 1 | PASS |
| 2 | `shift_handover_draft_lead_or_manager_insert` policy exists on `ShiftHandoverDraft` | 1 | 1 | PASS |
| 3 | `shift_handover_draft_lead_or_manager_update` policy exists on `ShiftHandoverDraft` | 1 | 1 | PASS |
| 4 | Insert policy is `FOR INSERT TO authenticated` (not `TO app_runtime`) | `{authenticated} / INSERT` | `{authenticated} / INSERT` | PASS |
| 5 | Update policy is `FOR UPDATE TO authenticated` | `{authenticated} / UPDATE` | `{authenticated} / UPDATE` | PASS |
| 6 | `authenticated` has INSERT+UPDATE on `ShiftHandoverDraft` (2 grants) | 2 | 2 | PASS |
| 7 | `app_runtime` has INSERT+UPDATE on `ShiftHandoverDraft` (2 grants) | 2 | 2 | PASS |
| 8 | B-1/B-2 baseline regression: 5 tables / 7 enums / 5 SELECT policies / 2 append-only triggers | 5/7/5/2 | 5/7/5/2 | PASS |
| 9 | `AutomationDecision` BEFORE UPDATE / BEFORE DELETE triggers still present | 2 | 2 | PASS |
| 10 | `app_runtime` still has `rolbypassrls = f` (per ADR-0017) | `rolbypassrls = f` | `rolbypassrls = false` | PASS |
| 11 | Existing 4 inventory tables untouched (`InventoryItem`, `InventoryMovement`, `InventoryStockSnapshot`, `WorkflowTask`) | 4 | 4 | PASS |
| 12 | Migration's `DO $$` regression guard would pass (4/4 assertions: table + 1 INSERT policy + 1 UPDATE policy + 1 `app_runtime` role) | 1/1/1/1 | 1/1/1/1 | PASS |

## Postgres version

From the owner's `psql "$DATABASE_URL" -c "SELECT version();"` (TBD — fill at apply time):

```
TBD
```

## Notes on the 12-query results

12/12 PASS, VERDICT: PASS, no regression to B-1/B-2 baseline; 2 new write policies on `ShiftHandoverDraft` are in effect; 2 new grants for `authenticated` + `app_runtime` are in effect; `app_runtime` retains `rolbypassrls = f`; 4 inventory tables untouched; migration's `DO $$` regression guard holds.

## Cross-references

- ADR-0025 (accepted) — the migration being promoted.
- ADR-0028 (accepted) — the sibling Phase B live-DB promotion on 2026-06-08 (same project, same 12-query shape).
- ADR-0029-A (accepted) — the sibling CUBE Venue-Model promotion on 2026-06-09 (same project, 14-query shape, scripts as direct siblings).
- `docs/DECISIONS.md` §ADR-0025 — the gate; defines the 2 new write policies + 2 grants + 3 endpoints.
- `docs/automation/promotion-evidence/RUNBOOK-adr-0025.md` — the 15-step procedure the owner follows.
- `scripts/verify-adr-0025-handover-draft-policies.ts` — the 12-query verification script (sibling of `scripts/verify-automation-phase-b-migrations.ts` and `scripts/verify-adr-0029a-operational-units.ts`).
- `docs/agent-team/mspr_logbook/2026-06-09-adr-0025-promotion.md` — the closure MSPR entry (to be flipped from `partial` to `pass` after the apply + verify pass).
- `docs/agent-team/mspr_logbook/2026-06-09-phase-e-handover-drafts.md` — the upstream E-1/E-3/E-4/E-5 closure entry that introduced the on-disk migration.

## Sign-off

- **Owner (apply authority):** cheikh.witm@proton.me — typed `npx prisma migrate deploy` at: 2026-06-09T03:37:42.226Z
- **Orchestrator (evidence recorder):** Mavis — recorded the 12-query output at: 2026-06-09T03:58:05.000Z

## Verdict

VERDICT: PASS — 12/12 verification gate clean. Phase E live-DB promotion complete on czinchfegtglmrloxlmh 2026-06-09.
