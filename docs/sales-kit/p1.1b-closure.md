# P1.1b Closure — Sales Kit Widerspruchsbereinigung & First Lead Dry Run

## Result

`partial`

Die fachlichen Gates dieses Slices sind erfüllt: W1–W4 sind geschlossen, O2 wurde für alle extern verwendbaren Produktclaims neu strukturiert und geprüft, der Skill-Layer ist konsistent verdrahtet und der synthetische Dry Run durchläuft alle sechs Skills fail-closed ohne Versand. Der Gesamtstatus bleibt `partial`, weil `npm run check:work-docs` trotz lokaler Modifikationen „nothing to check“ meldet und zwei externe/parallele Commits während des laufenden Slices eine saubere Abschlussbasis verändert haben.

## Geänderte Dateien

- Claim-/Mail-Texte: `first-contact-email.md`, `first-contact-final.md`, `follow-up-email.md`, `objection-handling.md`
- Lead/Gespräch/Offer: `lead-steckbrief-template.md`, `workflow-audit-template.md`, `pilot-conversation-guide.md`, `sales-call-guide.md`, `pilot-onepager.md`, `pilot-offer-light.md`
- SOT/Gates: `sales-positioning.md`, `outreach-readiness.md`, `sales-kit-index.md`
- Skill-Konsistenz: `skills/bevero-product-marketing-context.skill.md`
- Evidenz: `dry-runs/demo-first-lead-dry-run.md`, diese Closure, MSPR und Intent Memory

## W1–W4 Status

| Widerspruch | Status | Evidence |
|---|---|---|
| W1 Bar/Küche-Auffüllliste | `closed` | externe Texte nennen nur Bar-Auffüllliste; Küche nur als angrenzender Check-Prozess oder klare Nicht-Fähigkeit |
| W2 WhatsApp-Einfachheit | `closed` | Tatsachenclaim entfernt; Praktikabilität ist Pilotfrage |
| W3 Pilotumfang | `closed` | Onepager trennt Kandidaten-Menü von tatsächlichem 1–2-Prozess-Scope |
| W4 Export/Löschung | `closed` | Zusagen aus Einwandbehandlung entfernt; nur Stufe-1-Satz, O3 bleibt offen |

## O2 Status

- `confirmed`: K1–K5 und K9 in der jeweils erlaubten engen Form.
- `partial`: K6 Browser/Responsive-Struktur und K8 Küchen-Seiten; nur als technische bzw. abgrenzende Aussage zulässig.
- `unconfirmed`: K7 WhatsApp-Einfachheit; aus externen Texten entfernt.
- `future`: K10 Offline; nicht als aktuelle Fähigkeit verwendbar.
- Jede neue oder geänderte Produktbehauptung öffnet O2 erneut.

## Dry-Run Ergebnis

- Artefakt: `dry-runs/demo-first-lead-dry-run.md`
- Lead: vollständig fiktiver Demo-Betrieb ohne reale Person, Domain oder Kontaktadresse.
- Kette: Product Context → Lead Research → Operational Fit Score → Claim Red Team → Outreach Readiness → First Contact Draft → Final Readiness.
- Ergebnis: `pass` als Prozess-/Safety-Test; Test-Draft erzeugt, realer Versand wegen synthetischer Quelle und fehlendem Geschäftskanal korrekt blockiert.

## Versandstatus

`blocked / not_sent` — keine externe Aktion, kein Mail-Tool, keine Freigabe. Ein realer Versand benötigt einen neu recherchierten öffentlichen Lead, `ready_for_human_review` und menschliches Go.

## Pilotstartstatus

`blocked` — O1, O3, Runtime-Smoke und schriftlicher 1–2-Prozess-Scope sind noch nicht vollständig geschlossen.

## Offene Owner-Entscheidungen

- O1: Preis-/Pilotvariante final entscheiden.
- O3: Hosting/AVV/Export/Löschprozess belegen oder bewusst bei Stufe 1 bleiben; vor Pilotstart schließen.
- O5: superseded `sales-call-guide.md` löschen oder als Archiv behalten.
- Tracking: parallele Commits `47ca501` und `c851236` sowie verbleibenden lokalen Diff prüfen; bis dahin kein weiterer Commit/Push.

## Verification / Validation

- Aktive W1–W4-Überclaim-Muster: `pass` — keine verbotene aktive Formulierung in den extern nutzbaren Texten.
- O2-Statusvokabular und erlaubte/verbotene Formulierungen: `pass`.
- Vollständige Dry-Run-Kette + `blocked / not_sent`: `pass`.
- Sechs Skill-Dateien: Struktur, Guardrails und Index-Verdrahtung `pass`.
- RED/GREEN Skill-Drift-Test: `pass` nach minimaler Vokabular-Korrektur.
- Trailing whitespace: `pass`.
- `git diff --check`: `pass`.
- `npm run check:work-docs`: `fail` mit sichtbarer Diagnose; `getChangedFiles()` liefert 3 Pfade. Der erste Pfad verliert durch eine fehlerhafte `.trim()`-Stelle im Validator die ersten beiden Zeichen (off-by-one: Status-Prefix ` M ` wird vom trim weggefressen, slice(3) trifft `d` des Pfads). Folge: der MSPR-Eintrag erscheint als `ocs/agent-team/mspr_logbook/…` und matcht `newMsprEntries` nicht. Zusätzlich fehlt im Changeset ein neuer Intent-Eintrag. Das ist kein Sandbox-/Kindprozess-Problem mehr, sondern ein Skript-Bug + fehlender Intent-Eintrag; beide müssen vor Commit/Review explizit adressiert werden.

## Commit-Empfehlung

Während dieses Slices erschienen extern/parallel `47ca501` (`docs(sales-kit): add complete sales kit + production closure docs`) und `c851236` (`docs(sales-kit): finalize P1.1b contradiction cleanup + work documentation`) auf `main` und `origin/main`; dieser Agent hat weder committed noch gepusht. Empfehlung: jetzt **nicht** erneut committen oder pushen. Zuerst beide Commits und den verbleibenden lokalen Abschlussdiff owner-reviewen; danach Work-Docs-Check in einer Umgebung wiederholen, in der sein interner Git-Aufruf den Changeset lesen kann. Falls der Restdiff übernommen wird, nur die exakt geprüften Abschlussdateien in einen separaten Docs-Commit aufnehmen.

## Nächster sicherer Schritt

Owner reviewt `47ca501`, `c851236` und den verbleibenden Abschlussdiff. Danach Work-Docs-Check mit sichtbarem Changeset erneut ausführen; erst anschließend einen realen Lead aus öffentlichen Quellen bearbeiten.
