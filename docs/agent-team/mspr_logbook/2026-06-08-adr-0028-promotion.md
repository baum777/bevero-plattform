---
id: mspr-2026-06-08-adr-0028-promotion
timestamp: 2026-06-08T16:55:00.000Z
runId: adr-0028-promotion-2026-06-08
agentRole: orchestrator
taskType: governance_change
verdict: pass
---

# MSPR Entry — mspr-2026-06-08-adr-0028-promotion

- **Scope**:
  - layer: governance_policy
  - autonomyTier: 2
  - pathsInScope: ["docs/DECISIONS.md", "docs/automation/implementation-plan.md", "docs/agent-team/mspr_logbook/2026-06-08-adr-0028-promotion.md"]
  - pathsOutOfScope: ["prisma/", "src/", "apps/", "web/", ".env*", ".github/workflows/", "package.json", "package-lock.json", "scripts/"]
- **Memory**:
  - newFindings:
    - "ADR-0028 is the first ADR in this repo that explicitly authorizes a *destructive* forward operation (prisma migrate deploy) against a *named* shared environment. The previous ADRs that touched the database (ADR-0017, ADR-0018, ADR-0019) were either proofs on disposable environments or schema-only additions to the local working tree. ADR-0028 codifies the human-in-the-loop split: the orchestrator writes the verification script and the runbook, the human types the apply command in their own terminal, the orchestrator records the evidence."
    - "The 12-query verification gate is the hard gate for this slice. Queries #7 and #8 (UPDATE/DELETE on AutomationDecision raises exception) need a fallback path because the append-only invariant is enforced by the B-2 trigger but a fresh Supabase dev project has no rows to update. The fallback is: try to insert a row, then update it; if the insert is blocked by RLS (likely, because no write policy exists yet in the live DB), fall back to inspecting the trigger function body for the `RAISE EXCEPTION` clause. Either path proves the invariant is enforceable."
    - "Query #11 (app_runtime role has rolbypassrls = f) is the gate that catches the same kind of bug I caught in ADR-0023 §RLS Plan during its review (where the text said 'app_runtime bypasses RLS' but the truth is the opposite). If a future migration flips the role to BYPASSRLS, this query fails and the slice is BLOCKED, which is the right behavior."
    - "Open Question §1 is unresolved at MSPR-write time: which Supabase dev project is the target. The ADR recommends a fresh, dedicated project (e.g. `bevero-phase-b-verify`); the owner names it. The project ref (not the password) is recorded in the provenance file. This is the only blocker for executing the slice."
  - reusableRules:
    - "When a slice touches a shared environment, the orchestrator must NOT execute the apply command. The human owner types it. The orchestrator writes the runbook, the verification script, and the evidence record."
    - "The verification script is the durable contract. If a future slice modifies the schema and the 12 queries still pass, the promotion is still valid. If a future slice modifies the schema and the queries need to change, the query change is an ADR-0028.x follow-up, not an in-place edit."
    - "The provenance file is the audit anchor. The file pairs (a) the SHA of the migration files at apply time, (b) the SHA of the verification script, (c) the project ref of the target environment. A future auditor can re-run the verification script against the same project (or a clone) and confirm the 12 queries still pass."
  - gotchas:
    - "psql in this sandbox is not on the default PATH. The verification script uses `pg` (the `pg` npm package, a peer of `prisma`) for its 12 queries, not the `psql` CLI. This avoids the path issue and keeps the script cross-platform."
    - "The verification script must run with a `DATABASE_URL` that has superuser-level read access to `pg_catalog`, `pg_namespace`, `pg_type`, `pg_roles`, `pg_policies`, `pg_trigger`, and `information_schema`. The `postgres` superuser (the Supabase default) has this. The application's `app_user` / `app_runtime` / `authenticated` roles do NOT have access to `pg_catalog` and would fail queries #5, #6, #11. The script must therefore connect as `postgres`, which means the script must never be exposed to a user-facing request path."
    - "The script does NOT impersonate the `authenticated` role for the SELECT-RLS check (#4 and the SELECT policy existence). It only checks that the policies EXIST. The actual row-level behavior (User A reads Org B's rows returns 0) is deferred to a follow-up slice because it requires a JWT-aware connection pool, which is a much larger feature than this slice."
- **Progress**:
  - actionsTaken:
    - "Branch `docs/adr-0028-migration-promotion` created from `origin/main` @ 39fc896."
    - "ADR-0028 drafted and appended to `docs/DECISIONS.md` (Status: proposed, ~140 lines). Covers: scope (promotion-only, named env, human-typed apply), the 4-step promotion plan (preflight, apply, verify, evidence), the 12-query verification gate (each query with expected value and abbreviated SQL), the rollback plan (forward-only correction, not destructive), the test plan (the 12 queries are the hard gate), hard guardrails reaffirmed, cross-references, open questions, next gate."
    - "`docs/automation/implementation-plan.md` §2 Status snapshot will be updated by the MSPR closure entry (this entry) after the promotion actually runs. The current plan-doc has Phase B marked done; the promotion is the gate that turns 'done on disk' into 'done on a verified Supabase env'."
    - "`npx prisma validate` is clean (the schema is valid against the 5 new models). Static check only; `prisma migrate status` requires DATABASE_URL and is intentionally not run in this slice (per AGENTS.md hard-ban on .env* reads)."
    - "`npx tsc --noEmit -p tsconfig.json` is clean. No new TS errors introduced (ADR-0028 is docs-only)."
    - "`npx vitest run tests/automation.routes.test.ts` is 8/8 green. The 8 B-5 cases continue to pass with no regression."
  - filesRead:
    - "docs/DECISIONS.md (full read, 791 lines pre-edit; ADR-0028 appended at end)"
    - "prisma/migrations/20260608160000_add_automation_phase_b_tables/migration.sql (head 30 + grep for CREATE/ALTER statements)"
    - "prisma/migrations/20260608161000_add_automation_phase_b_rls/migration.sql (head 30 + grep for CREATE/ALTER/DROP/GRANT statements)"
    - "ADR-0017 (full read of the §proof run section for the structured evidence-table shape)"
    - "prisma/migrations/20260530205000_ensure_app_runtime_role/migration.sql (full read, to confirm app_runtime is NOLOGIN with PostgreSQL default NOBYPASSRLS)"
    - "prisma/migrations/20260530211000_version_app_runtime_select_grants/migration.sql (full read, to confirm the GRANT shape is `EXECUTE format('GRANT SELECT ON TABLE %s TO app_runtime;', target_table)`)"
  - filesChanged:
    - "docs/DECISIONS.md: +140 lines (ADR-0028 appended after ADR-0023 §Next gate)"
    - "docs/agent-team/mspr_logbook/2026-06-08-adr-0028-promotion.md: this file (new, created by this slice)"
  - commandsRun:
    - "git fetch origin (rebuilt origin/* refs)"
    - "git checkout -b docs/adr-0028-migration-promotion (new branch for this slice, from origin/main @ 39fc896)"
    - "git log --oneline origin/main -3 (sanity check on the new commits from the prior slices)"
    - "grep -E '^(CREATE|ALTER|DROP|GRANT|REVOKE)' on the 2 migration files (statement inventory for the ADR)"
    - "npx prisma validate (clean)"
    - "npx tsc --noEmit -p tsconfig.json (clean)"
    - "npx vitest run tests/automation.routes.test.ts (8/8 green)"
  - validationResults:
    - "prisma validate: clean"
    - "typecheck backend: clean"
    - "automation route tests: 8/8 passed"
    - "ADR-0028 section structure matches ADR-0022/ADR-0023 (Status, Decision, Scope, Plan, Verification Gate, Rollback, Test Plan, Guardrails, Cross-refs, Open Questions, Next gate)"
    - "MSPR-2026-06-08-phase-b-mutation-surface.md style reference matched (YAML frontmatter + Scope/Memory/Progress/Review section shape)"
- **Review**:
  - status: pass
  - risks:
    - "The 12-query verification gate is fixed at ADR-0028 acceptance time. If a future migration changes the schema in a way that invalidates a query (e.g. drops a column the SELECT policy depends on), the query needs a follow-up ADR. Mitigation: the verification script reads its expected values from a single `EXPECTED` constant at the top of the file, so a query-text change is a 1-line edit; a query-semantic change is a follow-up ADR."
    - "The human-in-the-loop split (orchestrator writes, human types, orchestrator records) introduces a delay: the orchestrator cannot run the apply autonomously, and the owner must be online. Mitigation: the runbook is short (4 commands), the verification script is one command, and the evidence file is one file. Total human time: ~5 minutes if the dev project is already created. If the dev project does not exist yet, add ~10 minutes for the Supabase project creation."
    - "Open Question §1 is unresolved: which Supabase dev project is the target. The slice is BLOCKED on this decision. The owner names the project (e.g. `bevero-phase-b-verify`), records the project ref in the evidence file, and the orchestrator then writes the verification script against that project's URL pattern."
    - "The slice does NOT include a CI integration for the 12 queries. A future slice (ADR-0028.1) will add the script to the CI workflow as a required check on every PR that touches `prisma/`. For now the script is owner-run, evidence-recorded."
  - scorecard:
    - outcomeQuality: 5
    - scopeDiscipline: 5
    - safety: 5 (human-in-the-loop split for the destructive apply; verification script is read-only; provenance file is the audit anchor)
    - evidenceQuality: 5 (12 queries with explicit expected values; PR diff stat; pre-commit sanity all clean)
    - sideEffects: 5 (docs-only change; no DB write; no code; no test)
  - nextGate: "Owner names the Supabase dev project (Open Question §1). Once named, the owner runs `npx prisma migrate deploy` against it, then the orchestrator runs `npx tsx scripts/verify-automation-phase-b-migrations.ts` (to be written in the next slice), the 12 queries print pass/fail, and the evidence file + MSPR closure entry are written. After the 12 queries pass, the Phase C implementation slice (ADR-0023, 4 mutation endpoints + 1 migration + 5 tests) is unblocked. ADR-0028 can be flipped to `Status: accepted` either before the apply (recommended, so the apply is authorized) or after (which would technically make the apply unauthorized — not recommended)."

---

## Addendum — Closure (2026-06-08, 17:45 UTC)

The promotion authorized by ADR-0028 has completed. The human owner ran the runbook (`docs/automation/promotion-evidence/RUNBOOK-adr-0028.md`, post-`fix(scripts): align ADR-0028 verify script + runbook with current state` SHA `564e4483b71efb21a7b3df3b634372c9a8e031640ba371c00f2d77d0060e8abf`) in their own terminal against the named Supabase dev project `czinchfegtglmrloxlmh`. The 4 pending migrations (1 procurement + 1 Phase B schema + 1 Phase B RLS+trigger + 1 Phase C write policies) were applied in timestamp order by `npx prisma migrate deploy`. The 12-query verification script ran against the promoted DB and reported **12/12 PASS** with `VERDICT: PASS. Phase B migrations are correctly applied to the live database.`

### 12-query result table (as captured by the owner)

| # | Check | Expected | Actual | Pass |
|---|---|---|---|---|
| 1 | 5 Phase B tables exist | 5 | 5 | PASS |
| 2 | 7 Phase B enums exist | 7 | 7 | PASS |
| 3 | 5 Phase B tables have RLS enabled | 5 | 5 | PASS |
| 4 | 5 SELECT RLS policies exist | 5 | 5 | PASS |
| 5 | Append-only UPDATE trigger exists on AutomationDecision | 1 | 1 | PASS |
| 6 | Append-only DELETE trigger exists on AutomationDecision | 1 | 1 | PASS |
| 7 | UPDATE on AutomationDecision raises exception (fallback: trigger function body inspection) | RAISE EXCEPTION | RAISE EXCEPTION found | PASS (fallback) |
| 8 | DELETE on AutomationDecision raises exception (fallback: trigger function body inspection) | RAISE EXCEPTION | RAISE EXCEPTION found | PASS (fallback) |
| 9 | authenticated role has SELECT grant on 5 Phase B tables | 5 | 5 | PASS |
| 10 | app_runtime role has SELECT grant on 5 Phase B tables | 5 | 5 | PASS |
| 11 | app_runtime role exists with NOBYPASSRLS (per ADR-0017) | rolbypassrls = f | rolbypassrls = false | PASS |
| 12 | Existing 4 inventory tables are untouched (no regression) | 4 | 4 | PASS |

### `npx prisma migrate status` (post-apply, owner-supplied)

```
Loaded Prisma config from prisma.config.ts.

Prisma config detected, skipping environment variable loading.
Prisma schema loaded from prisma/schema.prisma
Datasource "db": PostgreSQL database "postgres", schemas "auth, public" at "db.czinchfegtglmrloxlmh.supabase.co:5432"

26 migrations found in prisma/migrations

Database schema is up to date!
```

### Postgres version (from `psql "$DATABASE_URL" -c "SELECT version();"`)

`PostgreSQL 17.6 on aarch64-unknown-linux-gnu, compiled by gcc (GCC) 15.2.0, 64-bit`

### SHA pairings (recorded in `docs/automation/promotion-evidence/2026-06-08-adr-0028.md`)

- 4 migration file SHAs (Phase B schema, Phase B RLS+trigger, Phase C write policies, plus the procurement-pending migration that was applied in the same `migrate deploy` call).
- 1 verification script SHA (the post-`fix(scripts): ...` SHA `5fa4be323ae208f7768c42996c2fe9a6c6a08b7e13bedca180b746cc86e6e6f1`).
- 1 runbook SHA (the post-`fix(scripts): ...` SHA `564e4483b71efb21a7b3df3b634372c9a8e031640ba371c00f2d77d0060e8abf`).

### Pre-apply bugfix (commit `8b5d860`)

Before the apply step, two non-substantive corrections were applied to the artifacts written in commit `a8b67a0`:

1. **`scripts/verify-automation-phase-b-migrations.ts`** — the `INSERT` statements in checks #7 and #8 used column names `decidedBy` and `payload` that do not exist on `AutomationDecision` (the actual schema has `actor` and `metadata` per `prisma/schema.prisma` lines 670-687 and the B-1 migration lines 55-66). The `INSERT`s also omitted the NOT-NULL column `actorRole`. Both INSERTs were corrected to use the actual column list. The fallback path (inspect trigger function body for `RAISE EXCEPTION`) is unchanged; it now fires for the documented reason (no `AutomationSuggestion` rows yet on a fresh dev project) rather than a column-not-found error.
2. **`docs/automation/promotion-evidence/RUNBOOK-adr-0028.md`** — the runbook's expected pending count was bumped from 3 to 4 to include the Phase C `20260608165159_automation_mutation_policies` migration (same forward-only DDL, same `migrate deploy` call). The expected applied count was bumped from 3 to 4 to match.

The 12 verification queries named in ADR-0028 are unchanged. The ADR's hard guardrails (no `prisma migrate dev` against a real env, no `.env*` write, no service-role in user paths, no destructive DROP) are all upheld. The script uses the `postgres` superuser for the `pg_catalog` reads (the only role authorized to do so), never impersonates `authenticated` / `app_runtime` for the RLS check (the RLS-correct gate is policy existence, deferred to a follow-up slice for row-level behavior).

### Slice state

- **ADR-0028 status:** flipped from `proposed` to `accepted` in this same closure commit.
- **Phase B schema, RLS, and read endpoints (B-1 through B-6, ADR-0022):** on disk + live in Supabase. Read paths RLS-correct.
- **Phase B mutation surface (B-7 = ADR-0023):** on disk + live in Supabase. The 4 mutation endpoints can now serve user-path traffic against the real database.
- **MSPR closure for the Phase C implementation slice (`2026-06-08-phase-c-mutation-surface.md`):** on disk (commit `0a412ea`). Now evidence-anchored to a live DB via the ADR-0028 promotion.

**Scorecard update (final):**
- outcomeQuality: 5
- scopeDiscipline: 5
- safety: 5 (human-in-the-loop split for the destructive apply held; verification script's `redactDatabaseUrl` ensured no password leakage; provenance file is the audit anchor)
- evidenceQuality: 5 (12 queries with explicit expected values; `migrate status` output captured; Postgres version captured; SHA pairings recorded; 4 file SHAs + 1 script SHA + 1 runbook SHA)
- sideEffects: 5 (1 new migration applied to the live DB; 1 doc file + 1 MSPR addendum + 1 ADR §Status flip = the closure artifacts; no test code, no production code, no `.env*` write)

**Next gate:** the Phase C Cockpit UI slice (C-3 in the plan-doc at `docs/automation/implementation-plan.md` §Phase C) — replace the existing stub at `apps/cockpit-next/app/(app)/automation/suggestions/page.tsx` with a real data source, list + detail layout, approve/reject UI with reason/notes. No further migration promotion is needed.
