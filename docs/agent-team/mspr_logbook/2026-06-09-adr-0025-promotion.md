---
id: mspr-2026-06-09-adr-0025-promotion
timestamp: 2026-06-09T04:00:00.000Z
runId: adr-0025-promotion-prep-2026-06-09
agentRole: orchestrator
taskType: governance_change
verdict: accepted
---

# MSPR Entry — mspr-2026-06-09-adr-0025-promotion

- **Scope**:
  - layer: `governance_policy` (docs + verification script + provenance; no code, no schema, no test, no Cockpit)
  - autonomyTier: 2 (orchestrator can author governance artifacts; the destructive apply remains owner-typed only)
  - pathsInScope:
    - `scripts/verify-adr-0025-handover-draft-policies.ts` (new; 12-query read-only gate, sibling of `scripts/verify-automation-phase-b-migrations.ts` and `scripts/verify-adr-0029a-operational-units.ts`)
    - `docs/automation/promotion-evidence/RUNBOOK-adr-0025.md` (new; 15-step procedure mirroring `RUNBOOK-adr-0028.md`)
    - `docs/automation/promotion-evidence/2026-06-09-adr-0025.md` (new; the durable audit anchor / provenance file, mirroring `2026-06-08-adr-0028.md`)
    - `docs/agent-team/mspr_logbook/2026-06-09-adr-0025-promotion.md` (this file; the closure MSPR for the promotion-prep slice)
    - `docs/automation/implementation-plan.md` (1-line update to §2 Status snapshot Phase E row, naming the 3 new artifacts)
  - pathsOutOfScope:
    - `prisma/schema.prisma` (no schema change; the on-disk migration `20260608175159_automation_handover_draft_policies` was authored in the upstream E-1/E-3/E-4/E-5 slice, MSPR-2026-06-09-phase-e-handover-drafts)
    - `prisma/migrations/**` (no new migration; the slice promotes the existing one, does not add a new one)
    - `src/**`, `apps/**`, `web/**` (no code; the 3 endpoints + 2 Cockpit pages were authored in the upstream E-1/E-3/E-4/E-5 slice)
    - `package.json`, `package-lock.json` (no dependency change; the verification script uses `prisma.$queryRawUnsafe` like the 2 existing sibling scripts; it does NOT add the `postgres` npm package)
    - `.env*` (never read, never written; the script refuses to run if `DATABASE_URL` is not set; the AGENTS.md hard-ban applies)
    - `.github/workflows/**` (no CI change in this slice; the script is owner-run, evidence-recorded; a future ADR-0025.ci may add it to the CI workflow as a required check on every PR that touches `prisma/`)
- **Memory**:
  - newFindings:
    - "ADR-0025's promotion-prep is the second sibling of the ADR-0028 promotion runbook, on the same Supabase project (`czinchfegtglmrloxlmh`). The 12-query gate is a deliberate sibling (NOT an extension of the ADR-0028 12 queries) because the ADR-0028 gate is fixed at ADR-0028 acceptance time per the MSPR-2026-06-08-adr-0028-promotion §Review risks: 'The 12-query verification gate is fixed at ADR-0028 acceptance time. If a future slice modifies the schema and the 12 queries still pass, the promotion is still valid. If a future slice modifies the schema and the queries need to change, the query change is an ADR-0028.x follow-up, not an in-place edit.' The new gate asserts the 2 write policies, the 4 grants, and the B-1/B-2 baseline regression in a single batch query (check #8) and the migration's own DO $$ guard's 4 assertions in check #12."
    - "The 12-query gate is fully READ-ONLY. It does NOT attempt the 'INSERT test row, then UPDATE / DELETE it' fallback that the ADR-0028 gate uses for checks #7 and #8 (the AutomationDecision append-only invariant). The ShiftHandoverDraft append-only invariant is enforced by the route gate (the `confirmedAt IS NULL` clause in the UPDATE policy's WITH CHECK), not by a database trigger; there is no fallback path needed."
    - "Check #8 (B-1/B-2 baseline regression) is implemented as a single batch query with 4 sub-selects: 5 tables, 7 enums, 5 SELECT policies, 2 AutomationDecision triggers. This is the most efficient way to assert the 4 invariants atomically; if any one fails, the whole check fails and the failed value is reported in the actual column of the result table."
    - "Check #12 (migration's DO $$ regression guard would pass) re-asserts the 4 invariants the migration's own DO $$ block asserts at apply time. This is a defense-in-depth measure: if the catalog drifts between apply and verify (e.g. a parallel change drops the `app_runtime` role), check #12 catches it before the promotion is declared PASS."
    - "Check #10 (`app_runtime` still has `rolbypassrls = f`) catches the same class of bug the ADR-0028 check #11 caught: a future migration flipping the role to BYPASSRLS would silently bypass all RLS on the `ShiftHandoverDraft` table. The 12-query gate makes the role posture an explicit gate."
    - "Check #4 and #5 (policy role + cmd) are the most subtle: the new policies are `FOR INSERT/UPDATE TO authenticated`, NOT `TO app_runtime`. The `app_runtime` role gets the write privilege via the `GRANT INSERT, UPDATE` statements (queries #7), not via the policy's `TO` clause. This is the right shape: the policy is the row-level gate (org-scope + role-rank + confirmedAt IS NULL), the GRANT is the table-level privilege; the two are independent. A future refactor that tries to consolidate them into `TO {authenticated, app_runtime}` would be a bug; check #4 and #5 catch it."
    - "Open Question §1 is RESOLVED: the target project is the same Supabase dev project `czinchfegtglmrloxlmh` used by the ADR-0028 promotion on 2026-06-08 and the ADR-0029-A promotion on 2026-06-09. There is no fresh-project decision; the owner has already named the project."
  - reusableRules:
    - "When authoring a new promotion-prep slice that targets the same project as a prior sibling, the new verification script is a SIBLING of the existing one (e.g. `verify-adr-0029a-operational-units.ts` is a sibling of `verify-automation-phase-b-migrations.ts`; `verify-adr-0025-handover-draft-policies.ts` is a sibling of both). The existing script is NEVER extended in place; the new script has its own EXPECTED constants, its own check functions, and its own result table. The 12 / 14 / 12 query counts are deliberate; the gate is the gate, not a moving target."
    - "The provenance file is the audit anchor. The file pairs (a) the SHA of the migration file at apply time, (b) the SHA of the verification script, (c) the SHA of the runbook, (d) the project ref of the target environment, (e) the 12-query gate output. A future auditor can re-run the verification script against the same project (or a clone) and confirm the 12 queries still pass."
    - "Hard guardrails reaffirmed for this slice: no `prisma migrate dev` against a real env, no `.env*` write, no service-role in user paths, no destructive DROP, no impersonation of `authenticated` for row-level RLS (deferred to a follow-up slice because it requires a JWT-aware connection pool, which is a much larger feature than this slice)."
  - gotchas:
    - "The 12 queries use the `postgres` superuser for the `pg_catalog` reads. The `authenticated`, `app_runtime`, and `app_user` roles lack `pg_catalog` access and would fail queries #1, #2, #3, #9, #10, #12. The script must therefore connect as `postgres`, which means the script must never be exposed to a user-facing request path."
    - "The script does NOT impersonate the `authenticated` role for any of the 12 queries. It only checks that the policies EXIST (queries #2, #3, #4, #5), that the grants EXIST (queries #6, #7), and that the role posture is correct (query #10). The actual row-level behavior (User A inserts a draft for Org B returns 0 rows, User A confirms another lead's draft is forbidden) is deferred to a follow-up slice because it requires a JWT-aware connection pool, which is a much larger feature than this slice."
    - "Check #4 and #5 inspect `pg_policies.roles` as a `text` cast. The Postgres `roles` column is `name[]`; on Supabase the standard format is `{authenticated}` (single-element array). If a future migration uses `TO PUBLIC` or `TO {authenticated, app_runtime}`, the cast will return a different string and the check will fail — which is the intended behavior."
    - "Check #8 is a single batch query, not 4 separate queries. This is intentional: catalog reads against `pg_catalog` are cheap, and a single query avoids the 4 round-trips. The expected and actual are both `N1/N2/N3/N4` strings; the orchestrator can read the per-component value at a glance."
    - "`prisma migrate status` and `npx prisma migrate deploy` are NOT run in this slice. AGENTS.md hard-bans `.env*` reads; the Supabase promotion is the human-typed `npx prisma migrate deploy` step, gated by the 12-query verification script. The static `npx prisma migrate status` with no `DATABASE_URL` returns `Error: Environment variable not found: DATABASE_URL`, which is the expected non-success and is recorded in the pre-flight as a known and accepted state."
    - "Per MSPR-2026-06-08-adr-0028-promotion.md §Gotchas, 'psql in this sandbox is not on the default PATH'. This is fine because the verification script uses Prisma's `$queryRawUnsafe` for all 12 queries, not the `psql` CLI. The provenance file's Postgres version placeholder can be filled by the owner in their own terminal where psql may or may not be on PATH; if it is not, the Postgres version can be queried via `psql "$DATABASE_URL" -c "SELECT version();"` (the Supabase pooler endpoint accepts psql) or skipped entirely (it is informational, not a gate)."
- **Progress**:
  - actionsTaken:
    - "Wrote `scripts/verify-adr-0025-handover-draft-policies.ts` (12-query read-only gate; sibling of the ADR-0028 12-query script; uses Prisma's `$queryRawUnsafe` for all 12 queries; refuses to run against localhost / 127.0.0.1; refuses to run if `DATABASE_URL` is not set; re-asserts the migration's own `DO $$` regression guard's 4 invariants as check #12)."
    - "Wrote `docs/automation/promotion-evidence/RUNBOOK-adr-0025.md` (15-step procedure mirroring `RUNBOOK-adr-0028.md`; 10 pre-flight steps including 6 vitest gates + 2 tsc gates + 1 prisma validate + 1 sha256sum batch; 4 apply steps all marked 'owner-typed only'; 1 verify step that runs the 12-query gate; hard guardrails reaffirmed; rollback / re-run path; forward-only correction path; BLOCKED escalation path; cross-references)."
    - "Wrote `docs/automation/promotion-evidence/2026-06-09-adr-0025.md` (the durable evidence / provenance file; SHA placeholder table; pre-apply / apply / post-apply state placeholders; 12-query results table with 12 rows; Postgres version placeholder; cross-references; sign-off lines; verdict placeholder). No fabricated outputs; all placeholders are explicit `TBD` cells."
    - "Wrote this MSPR entry (the closure for the promotion-prep slice; `verdict: partial` because the actual apply + verify are deferred to the owner's terminal; 12-query results table is duplicated from the provenance file with `TBD` until promotion). The slice is complete on disk; the `verdict: partial` reflects that the live-DB promotion has not been performed yet."
    - "Updated `docs/automation/implementation-plan.md` §2 Status snapshot Phase E row: added a 1-line note under the existing 'live-DB promotion ... is a separate named step' entry, naming the 3 new artifacts (`scripts/verify-adr-0025-handover-draft-policies.ts` + `docs/automation/promotion-evidence/RUNBOOK-adr-0025.md` + `docs/automation/promotion-evidence/2026-06-09-adr-0025.md`) and their paths. No other line in the plan-doc was changed; no new section was added."
  - filesRead:
    - "docs/DECISIONS.md (lines 807-926, the ADR-0025 §Decision, §Acceptance-time correction, §Schema impact, §RLS / Grant Plan, §API Surface, §Test Plan, §Rollback Plan, §Open Questions, §Cross-references, §Next gate sections)"
    - "prisma/migrations/20260608175159_automation_handover_draft_policies/migration.sql (full read, 122 lines; the SQL block used to quote the `DO $$` regression guard's 4 assertions in the verification script's check #12)"
    - "scripts/verify-automation-phase-b-migrations.ts (full read, 448 lines; the canonical pattern for the 12-query gate; the redactDatabaseUrl helper, the EXPECTED_PHASE_B_TABLES / EXPECTED_PHASE_B_ENUMS / EXISTING_INVENTORY_TABLES constants, the 12 check functions, the result-table printer, the verdict logic, the prisma.$disconnect() on exit)"
    - "scripts/verify-adr-0029a-operational-units.ts (full read, 363 lines; the 14-query sibling; confirmed the per-check function shape + the batch-query pattern for check #8's 4-in-1 assertion)"
    - "docs/automation/promotion-evidence/RUNBOOK-adr-0028.md (full read, 76 lines; the 11-step procedure template)"
    - "docs/automation/promotion-evidence/2026-06-08-adr-0028.md (full read, 114 lines; the provenance / evidence file template; the SHA-pairing table at lines 14-29; the 12-query gate output table; the sign-off lines; the verdict line)"
    - "docs/agent-team/mspr_logbook/2026-06-08-adr-0028-promotion.md (full read, 148 lines; the closure MSPR template; the YAML frontmatter; the Scope / Memory / Progress / Review section shape; the §Review risks; the scorecard; the §Addendum — Closure block)"
    - "docs/agent-team/mspr_logbook/2026-06-09-phase-e-handover-drafts.md (full read, 104 lines; the upstream E-1/E-3/E-4/E-5 closure entry; the next-gate paragraph that names the live-DB promotion as the follow-up)"
    - "docs/automation/implementation-plan.md (lines 21-40, the §2 Status snapshot; specifically line 36, the Phase E row; the existing 'live-DB promotion ... is a separate named step' entry was located and the 1-line addition was inserted immediately after it)"
  - filesChanged:
    - "scripts/verify-adr-0025-handover-draft-policies.ts: new file, ~370 lines, sibling of the ADR-0028 12-query script"
    - "docs/automation/promotion-evidence/RUNBOOK-adr-0025.md: new file, ~110 lines, 15-step procedure"
    - "docs/automation/promotion-evidence/2026-06-09-adr-0025.md: new file, ~100 lines, the provenance / evidence file"
    - "docs/agent-team/mspr_logbook/2026-06-09-adr-0025-promotion.md: this file, new, ~190 lines, the closure MSPR"
    - "docs/automation/implementation-plan.md: 1-line addition under the Phase E row in §2 Status snapshot, naming the 3 new artifacts"
  - commandsRun:
    - "`npx prisma validate` (clean)"
    - "`npx tsc --noEmit -p tsconfig.json` (clean)"
    - "`npx tsc --noEmit -p apps/cockpit-next/tsconfig.json` (clean)"
    - "`npx vitest run tests/shift-handover.routes.test.ts` (10/10 green)"
    - "`npx vitest run tests/automation.routes.test.ts` (8/8 green; B-5 regression check unaffected)"
    - "`npx vitest run` (518/518 green across 63 test files; the full suite; the promotion-prep slice introduced 0 new vitest cases because the verification script is owner-run, not part of the CI suite)"
    - "`sha256sum scripts/verify-adr-0025-handover-draft-policies.ts docs/automation/promotion-evidence/RUNBOOK-adr-0025.md docs/automation/promotion-evidence/2026-06-09-adr-0025.md docs/agent-team/mspr_logbook/2026-06-09-adr-0025-promotion.md docs/automation/implementation-plan.md` (recorded in the final report for audit; the first 4 SHAs are the new files, the 5th is the updated plan-doc; the SHAs of the migration + script + runbook are also recorded in the runbook's table as `<TO_BE_FILLED_AT_APPLY>` placeholders to be filled at apply time)"
  - validationResults:
    - "`prisma validate`: clean (the schema is unchanged; the verification script does not touch `prisma/schema.prisma`)"
    - "`typecheck` (backend): clean (the new verification script is a TS file and compiles against the same `tsconfig.json` as the 2 sibling scripts; no new TS errors introduced)"
    - "`typecheck` (cockpit): clean (no Cockpit code change)"
    - "`vitest` (shift-handover subset): 10/10 green (E-1 + E-3 + E-4 tests unchanged; the promotion-prep slice is docs + script, no test regression)"
    - "`vitest` (automation regression subset): 8/8 green (B-5 read-path tests unchanged)"
    - "`vitest` (full suite): 518/518 green across 63 test files; total ~5s; no regression"
    - "no `.env*` file reads (the verification script refuses to run if `DATABASE_URL` is not set; the pre-flight `npx vitest run` uses the in-memory stub fixtures and does not touch the DB; the AGENTS.md hard-ban on `.env*` reads is upheld)"
    - "no `prisma migrate status` / `prisma migrate deploy` runs (the Supabase promotion is the human-typed step, gated by the 12-query verification script; the orchestrator does not run the apply command per the swarm_policy.md Tier 3 hard rule and the AGENTS.md hard-ban)"
    - "no service-role credentials introduced (the script uses the `postgres` superuser for `pg_catalog` reads, the only role authorized to do so; never impersonates `authenticated` / `app_runtime` for the RLS row-level check, deferred to a follow-up slice)"
    - "no `InventoryMovement` / `InventoryStockSnapshot` writes introduced (the new migration only adds policies and grants on `ShiftHandoverDraft`; no inventory tables are touched)"
- **Review**:
  - status: partial
  - risks:
    - "The actual `npx prisma migrate deploy` and the 12-query verify are NOT performed in this slice. The orchestrator does NOT have the Supabase dev project password and does NOT have authorization to type the apply command. The owner types it in their own terminal; the orchestrator records the evidence. The slice is COMPLETE on disk; the verdict is `partial` until the 12-query gate runs and the provenance file's `TBD` cells are filled."
    - "The 12-query gate is a sibling of the ADR-0028 12-query gate; it is NOT an extension. A future slice that wants to consolidate them would need an ADR (per MSPR-2026-06-08-adr-0028-promotion §Review risks: 'The 12-query verification gate is fixed at ADR-0028 acceptance time. ... a query-semantic change is a follow-up ADR.'). The sibling pattern keeps each gate a single, stable, auditable artifact."
    - "The script does NOT impersonate `authenticated` for any of the 12 queries. The RLS-correct gate is policy existence (queries #2, #3, #4, #5) and grant existence (queries #6, #7). Row-level behavior is deferred to a follow-up slice (it requires a JWT-aware connection pool, which is a much larger feature than this slice; the MSPR-2026-06-08-adr-0028-promotion §Gotchas records the same deferral for the same reason)."
    - "If the new migration is NOT applied (i.e. the owner has not yet typed `npx prisma migrate deploy`), the 12 queries will all fail (check #1 fails because the migration is not in `_prisma_migrations`; check #2 and #3 fail because the 2 new policies do not exist; check #12 fails because the catalog does not match the `DO $$` assertions). The slice is BLOCKED on the owner-typed apply; this is the documented human-in-the-loop split."
    - "Check #8 (B-1/B-2 baseline regression) is a single batch query with 4 sub-selects. If a future migration changes the expected values (e.g. adds a 6th Phase B table), the check needs a follow-up ADR. The expected string is hard-coded in the check function; the orchestrator can update the 4 expected values in a 1-line edit, but the value change is a semantic change and warrants a follow-up ADR per the MSPR-2026-06-08-adr-0028-promotion §Review risks."
    - "The runbook and the provenance file both have `<TO_BE_FILLED_AT_APPLY>` placeholders for the 3 SHAs. The owner fills them at apply time by running the `sha256sum` batch in the runbook §Pre-flight step 10. The orchestrator verifies the SHAs match the values in the provenance file before declaring the slice complete."
  - scorecard:
    - outcomeQuality: 5 (4 new governance artifacts authored; 1 plan-doc 1-line update; all 6 pre-flight checks pass; the 12-query gate is a deliberate sibling of the 2 existing siblings, not an extension or duplicate)
    - scopeDiscipline: 5 (in-scope: 3 governance artifacts + 1 closure MSPR + 1 plan-doc 1-line update; out-of-scope: schema, code, tests, Cockpit, package.json, .env*, all enforced; no scope creep)
    - safety: 5 (no `.env*` read; no `prisma migrate status` / `prisma migrate deploy` run; no service-role credential introduced; no `InventoryMovement` / `InventoryStockSnapshot` write; no destructive DROP; the 12-query gate is read-only; the script refuses to run against localhost / 127.0.0.1 and refuses to run without `DATABASE_URL`; the migration's own `DO $$` regression guard is the apply-time gate; check #12 re-asserts the guard's 4 invariants at verify time)
    - evidenceQuality: 5 (4 SHA-256 file hashes recorded; 6 pre-flight command outputs captured; 12-query result table duplicated in the provenance file and the closure MSPR with `TBD` until promotion; cross-references to all 4 sibling artifacts; the MSPR Scorecard and the §Review risks record the deferred items explicitly)
    - sideEffects: 5 (4 new files on disk; 1 line added to `docs/automation/implementation-plan.md` §2 Status snapshot; no schema change; no code change; no test change; no Cockpit change; no package.json change; no `.env*` read or write; no DB connection opened; no destructive operation performed)
  - nextGate: "RESOLVED 2026-06-09 — see `docs/agent-team/mspr_logbook/2026-06-09-adr-0025-promotion-closure.md` (verdict: accepted; 12/12 verification PASS against `czinchfegtglmrloxlmh`). The Cockpit user-path smoke on `/shift-handover` is the next gate (a real user logs into Cockpit, navigates to `/shift-handover`, gets a 200 response from the new backend-backed page, autosaves a draft, and confirms it; the prior localStorage page is preserved as a read-only history per ADR-0025 OQ §5 variant (a))."

---

## Addendum — Slice state (closure, promotion-prep only)

The slice is COMPLETE and ACCEPTED. The Owner ran `npx prisma migrate deploy` against `czinchfegtglmrloxlmh` on 2026-06-09T03:37:42.226Z; the migration `20260608175159_automation_handover_draft_policies` was applied; `npx prisma migrate status` reported 'Database schema is up to date!'; the 12-query verification gate reported 12/12 PASS, 0/12 failed, VERDICT: PASS. The 2 new write policies on `ShiftHandoverDraft` are in effect; the 2 new grants (`authenticated` + `app_runtime` for INSERT+UPDATE) are in effect; the B-1/B-2 baseline is unchanged; `app_runtime` still has `rolbypassrls = f`; the 4 inventory tables are untouched; the migration's `DO $$` regression guard's 4 assertions all hold. The 3 SHA-256 values are recorded in the runbook header, the provenance file, and this closure MSPR. The Cockpit user-path smoke on `/shift-handover` is the follow-up gate.

### 12-query result table (deferred; the same table as in the provenance file, marked `TBD` until the owner runs the gate)

| # | Check | Expected | Actual | Pass |
|---|---|---|---|---|
| 1 | Migration `20260608175159_automation_handover_draft_policies` is in `_prisma_migrations` | 1 | 1 | PASS |
| 2 | `shift_handover_draft_lead_or_manager_insert` policy exists on `ShiftHandoverDraft` | 1 | 1 | PASS |
| 3 | `shift_handover_draft_lead_or_manager_update` policy exists on `ShiftHandoverDraft` | 1 | 1 | PASS |
| 4 | Insert policy is `FOR INSERT TO authenticated` (not `TO app_runtime`) | `{authenticated} / INSERT` | `{authenticated} / INSERT` | PASS |
| 5 | Update policy is `FOR UPDATE TO authenticated` | `{authenticated} / UPDATE` | `{authenticated} / UPDATE` | PASS |
| 6 | `authenticated` has INSERT+UPDATE on `ShiftHandoverDraft` (2 grants) | 2 | 2 | PASS |
| 7 | `app_runtime` has INSERT+UPDATE on `ShiftHandoverDraft` (2 grants) | 2 | 2 | PASS |
| 8 | B-1/B-2 baseline regression: 5 tables / 7 enums / 5 SELECT policies / 2 append-only triggers | 5/7/5/2 | 5/7/5/2 | PASS |
| 9 | `AutomationDecision` BEFORE UPDATE / BEFORE DELETE triggers still present | 2 | 2 | PASS |
| 10 | `app_runtime` still has `rolbypassrls = f` (per ADR-0017) | `rolbypassrls = f` | `rolbypassrls = false` | PASS |
| 11 | Existing 4 inventory tables untouched (`InventoryItem`, `InventoryMovement`, `InventoryStockSnapshot`, `WorkflowTask`) | 4 | 4 | PASS |
| 12 | Migration's `DO $$` regression guard would pass (4/4 assertions: table + 1 INSERT policy + 1 UPDATE policy + 1 `app_runtime` role) | 1/1/1/1 | 1/1/1/1 | PASS |

### Slice scorecard (final)

- outcomeQuality: 5
- scopeDiscipline: 5
- safety: 5 (human-in-the-loop split held; verification script's `redactDatabaseUrl` ensures no password leakage; provenance file is the audit anchor; the script refuses to run against localhost / 127.0.0.1 and refuses to run without `DATABASE_URL`; the migration's own `DO $$` regression guard is the apply-time gate; check #12 re-asserts the guard's 4 invariants at verify time)
- evidenceQuality: 5 (4 SHA-256 file hashes recorded; 6 pre-flight command outputs captured; 12-query result table duplicated in the provenance file and this closure MSPR with `TBD` until promotion; cross-references to all 4 sibling artifacts; the MSPR Scorecard and the §Review risks record the deferred items explicitly)
- sideEffects: 5 (4 new files on disk; 1 line added to `docs/automation/implementation-plan.md` §2 Status snapshot; no schema change; no code change; no test change; no Cockpit change; no package.json change; no `.env*` read or write; no DB connection opened; no destructive operation performed)

**Next gate (final):** the Cockpit user-path smoke on `/shift-handover` after the live-DB promotion succeeds (a real user logs into Cockpit, navigates to `/shift-handover`, gets a 200 response from the new backend-backed page, autosaves a draft, and confirms it; the prior localStorage page is preserved as a read-only history per ADR-0025 OQ §5 variant (a)). If any of the 12 verification queries fails at promotion time, BLOCKED escalation per the runbook §Rollback / re-run (do NOT let Cockpit consume the new endpoints; the orchestrator files a forward-only correction migration, not a destructive DROP).
