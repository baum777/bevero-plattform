---
id: mspr-2026-06-08-phase-c-cockpit-ui-c3b-c4
timestamp: 2026-06-08T20:30:00.000Z
runId: phase-c-cockpit-ui-c3b-c4-2026-06-08
agentRole: orchestrator
taskType: implementation
verdict: pass
---

# MSPR Entry — mspr-2026-06-08-phase-c-cockpit-ui-c3b-c4

- **Scope**:
  - layer: `implementation` (frontend + dashboard + small backend helper; no migration; no RLS change; no governance change beyond plan-doc + ADR-0029)
  - autonomyTier: 1 (frontend slice that consumes the existing backend API surface; no schema, no RLS, no new endpoint, no external system)
  - pathsInScope:
    - `apps/cockpit-next/app/(app)/automation/suggestions/page.tsx` (rewritten: server component, RLS-scoped read via `listAutomationSuggestions`, role gate via `listWorkspaceSummariesForCurrentUser`, `AccessDenied` for viewer and unauthenticated, `EmptyState` / `ErrorState` / `SuggestionsClient` rendering)
    - `apps/cockpit-next/app/(app)/automation/suggestions/suggestions-client.tsx` (new: client component, list cards, filter bar with status + type, detail modal with role-aware Approve/Reject buttons, reason field with required-on-reject, optimistic local removal + `router.refresh()`)
    - `apps/cockpit-next/app/(app)/automation/suggestions/suggestions-stub.tsx` (deleted: superseded by `suggestions-client.tsx`; the prior stub was a deliberate placeholder created by commit `2d258e1` per the plan-doc §Phase C preview)
    - `apps/cockpit-next/app/(app)/dashboard/page.tsx` (extended: added the "Automation-Vorschläge" card between the "Alerts offen" and "Alerts gelöst" cards, using the new `data.openSuggestions` field, with a link to `/automation/suggestions` and German copy that adapts to the count)
    - `apps/cockpit-next/lib/supabase/queries/automation-suggestions.ts` (new: `AutomationSuggestionRow` + `AutomationSuggestionListResult` types; `listAutomationSuggestions` query with Zod-equivalent filters; `getAutomationSuggestion` for the detail; `countOpenAutomationSuggestions` for the dashboard card; `AUTOMATION_SUGGESTION_STATUSES` / `AUTOMATION_SUGGESTION_TYPES` exports)
    - `apps/cockpit-next/lib/supabase/queries/dashboard.ts` (extended: imports `countOpenAutomationSuggestions`, adds `openSuggestions: number | null` to `DashboardSnapshot`, wires the count in parallel with the other dashboard queries, gates the field on `workspacesResult.organizationId` being non-null)
    - `docs/automation/implementation-plan.md` (§2 Status snapshot: add the C-3b + C-4 row; flip the C-3 row from "blocked on C-3a" to "done")
  - pathsOutOfScope: `prisma/`, `src/` (backend), `api/`, `web/`, `.env*`, `.github/workflows/`, `package.json`, `package-lock.json`, `scripts/`, the local `c` scratch file at the repo root, any production DB, any external system, any LLM call, any service-role credential, any new migration, any new RLS policy

- **Memory**:
  - newFindings:
    - "**Cockpit read path uses Supabase-direct, not the backend proxy.** The dashboard pattern (`apps/cockpit-next/app/(app)/dashboard/page.tsx`) is a server component that calls `getDashboardSnapshot()` from `lib/supabase/queries/dashboard.ts`, which uses the RLS-scoped Supabase server client (`createClient()` from `lib/supabase/server.ts`). It does **not** call the Fastify backend. The B-2 RLS policy `automation_suggestion_org_member_select` is the org-scope gate; the C-3a backend read endpoints (commit `5507df6`) are still useful for external clients and for the test contract, but the Cockpit reads from Supabase directly for consistency with the dashboard. This is the right pattern for the Cockpit: server-side reads with RLS as the gate, client-side mutations via the backend proxy (`/api/backend/...`). The C-3a backend endpoints are the canonical external API surface; the Cockpit is one of multiple consumers."
    - "**Cockpit mutations use the existing backend proxy at `/api/backend/[...path]/route.ts`.** The proxy forwards `authorization`, `content-type`, `x-organization-id` headers, with a `BACKEND_API_BASE_MISSING` fallback that returns 503. The Cockpit appends `X-Organization-Id` from the `useAuth` context, fetches the Supabase session token from the client, and uses the same `fetch(\"/api/backend/...\", { headers })` pattern as the existing `app-shell.tsx` (bar-refill prewarm) and `registered-member-approval-form.tsx` (team member approval). The new `suggestions-client.tsx` mirrors this pattern for Approve and Reject."
    - "**Spec text wins over plan-doc hint on the role-aware filter.** The plan-doc §Phase C C-2 hints at 'role-aware: staff sees own; manager+ sees all in org.' The spec text (`docs/automation/semi-automated-operations-layer.md` line 751) only says 'Auth: Required (staff+).' The shipped slice implements the spec text: staff+ can see all suggestions in the org, with no per-role filter. The role-aware filter is a follow-up ADR per the C-3a MSPR + ADR-0029 open-questions section. The Cockpit slice does not need to wait for the role-aware filter; it ships the spec text."
    - "**German-localized UI matches the existing Cockpit voice.** The Cockpit's existing pages (dashboard, alerts, storage, etc.) are German-localized. The new suggestions page and the new dashboard card use the same voice: 'Vorschlag freigeben', 'Begründung', 'Keine Vorschläge', 'Aktuell liegen keine offenen Vorschläge vor. Sobald der Rules Engine einen niedrigen Bestand, einen offenen Wareneingang oder eine Verbrauchsanomalie erkennt, erscheint hier ein Vorschlag zur Freigabe.' The new types (refill, receipt_alert, consumption_anomaly, alert_consolidation) are surfaced in German (Refill, Wareneingang offen, Verbrauchsanomalie, Alert-Bündelung). The English backend enum values are unchanged."
    - "**Mobile-first via existing CSS classes.** The Cockpit's existing CSS classes (`surface-card`, `state-list`, `toolbar-row`, `state-action-row`, `state-panel`) handle the mobile-first layout per the spec's 'full-width cards on mobile' requirement. No new CSS was added; the new pages use the same classes as the alerts and storage pages. The spec's 'swipe-left for action sheet' and 'bottom sheet for detail' are out of scope for C-3b; the existing modal pattern (`state-panel`) is the desktop/tablet detail and the cards are full-width on mobile. A future slice can add the swipe-left action sheet as a UX enhancement."
  - reusableRules:
    - "When the Cockpit needs a new server-side read, mirror the dashboard pattern: server component -> RLS-scoped Supabase query via `lib/supabase/server.ts` -> typed result. The query helper goes in `lib/supabase/queries/<name>.ts` with a typed row + a typed return. The page imports the query helper, calls it server-side, and renders the result. Mutations go client-side via the `/api/backend/...` proxy."
    - "When the Cockpit needs a new mutation, mirror the `registered-member-approval-form.tsx` pattern: client component -> `fetch('/api/backend/<path>', { method, headers: { Authorization: Bearer <token>, X-Organization-Id: <orgId> } })` -> optimistic local update + `router.refresh()` -> handle the response status and the error JSON shape. The backend proxy at `apps/cockpit-next/app/api/backend/[...path]/route.ts` handles header forwarding and the 503 fallback."
    - "When the Cockpit replaces a stub (e.g. suggestions-stub.tsx), the new client component should be a separate file (e.g. suggestions-client.tsx) imported by the server-component page. The page does the data fetch + role gate; the client component does the rendering + interactions. The prior stub file is deleted in the same commit, not kept as a fallback."
  - gotchas:
    - "**Relative import paths in the Cockpit.** The Cockpit is a Next.js app under `apps/cockpit-next/`. From `apps/cockpit-next/app/(app)/automation/suggestions/page.tsx`, the path to `apps/cockpit-next/lib/supabase/queries/...` is `../../../../lib/supabase/queries/...` (4 levels up: `suggestions/` -> `automation/` -> `(app)/` -> `app/` -> `apps/cockpit-next/`). The 3-level path (`../../../`) is a common off-by-one in this stack; typecheck catches it immediately."
    - "**Supabase `count` semantics for the dashboard card.** `countOpenAutomationSuggestions` uses `count: \"exact\", head: true` which returns the row count without fetching the rows. This is the right pattern for a dashboard count; the alternative (`.select(\"*\")` with a count) would fetch the row data unnecessarily. The function returns `0` on error (rather than throwing) so the dashboard always renders a number, even if the count query fails."
    - "**The `useAuth` context provides `organizationId` for the X-Organization-Id header.** The suggestions-client fetches via the backend proxy, which forwards the X-Organization-Id header to the Fastify backend. The backend's `parseActorFromHeaders` reads the header to select the actor's org. The Cockpit passes `organizationId` from the server-rendered `workspaces.organizationId` (via the page prop) to the client component, which then sets the header on every mutation. The Cockpit's auth provider (which lives in the browser) is used for the session token, not for the org id (which is determined server-side per request)."
    - "**`router.refresh()` after a mutation re-runs the server component.** The suggestions-client calls `router.refresh()` after a successful Approve/Reject, which re-fetches the server-component page and re-runs the RLS-scoped Supabase query. The locally-removed suggestion (via `setRemovedIds`) is also gone from the server response, so the visible list shrinks correctly. The pattern matches `alerts-table-client.tsx`."
- **Progress**:
  - actionsTaken:
    - "Read the existing Cockpit patterns: `apps/cockpit-next/app/(app)/dashboard/page.tsx` (server component + RLS-scoped Supabase query), `apps/cockpit-next/app/(app)/alerts/alerts-table-client.tsx` (client component + fetch to `/api/alerts/...`), `apps/cockpit-next/app/(app)/settings/team/registered-member-approval-form.tsx` (client component + fetch to `/api/backend/...` with auth header), `apps/cockpit-next/app/api/backend/[...path]/route.ts` (the backend proxy), `apps/cockpit-next/lib/supabase/server.ts` (the server-side Supabase client), `apps/cockpit-next/lib/supabase/queries/dashboard.ts` (the existing dashboard query helper pattern)."
    - "Wrote `apps/cockpit-next/lib/supabase/queries/automation-suggestions.ts` (~150 lines): `AutomationSuggestionRow` + `AutomationSuggestionListResult` types, `listAutomationSuggestions` (RLS-scoped Supabase query with status/type/ruleId filters, `createdAt DESC` ordering, limit/offset pagination, exact count via `{ count: \"exact\" }`), `getAutomationSuggestion` (single row by id + org), `countOpenAutomationSuggestions` (count-only for the dashboard card), `AUTOMATION_SUGGESTION_STATUSES` / `AUTOMATION_SUGGESTION_TYPES` exports. Mirrors the existing inventory / storage / workspaces query patterns."
    - "Extended `apps/cockpit-next/lib/supabase/queries/dashboard.ts`: imported `countOpenAutomationSuggestions`, added `openSuggestions: number | null` to `DashboardSnapshot`, wired the count into the parallel `Promise.all` block (depends on `workspacesResult.organizationId`), gated the field on the org id being non-null. The dashboard data shape is a strict superset of the prior shape; consumers that don't read `openSuggestions` are unaffected."
    - "Added the 'Automation-Vorschläge' card to `apps/cockpit-next/app/(app)/dashboard/page.tsx` between the 'Alerts offen' and 'Alerts gelöst' cards. The card shows the count via `data.metric-value` (matching the existing `mono metric-value` class), a German copy string that adapts to the count (0 / 1 / N), a link to `/automation/suggestions`, and a badge variant that switches between `warning` (>0 open), `ok` (0 open), and `neutral` (N/A, e.g. when the org id is null). The card is part of the same `grid-2 dashboard-grid-3` container; the existing 5 cards remain in place."
    - "Rewrote `apps/cockpit-next/app/(app)/automation/suggestions/page.tsx`: now a server component that (a) reads `listWorkspaceSummariesForCurrentUser` for the actor's org + role, (b) returns `AccessDenied` for viewer / unauthenticated, (c) parses the `status` and `type` search params via local Zod-equivalent helpers, (d) calls `listAutomationSuggestions` with the parsed filters, (e) returns `ErrorState` / `EmptyState` / `SuggestionsClient` based on the result. The page is `export const dynamic = \"force-dynamic\"` (matches the prior stub) so search params are always read fresh."
    - "Wrote `apps/cockpit-next/app/(app)/automation/suggestions/suggestions-client.tsx` (~280 lines): the client-side list + filter + detail + actions. Renders a filter bar (status + type selects that navigate to the same page with updated search params via `router.push`), a list of cards (title, rule-id + version, type + createdAt, status badge, related item ids, Approve/Reject buttons when `canApprove && status === 'open'`), a detail modal (full suggestion fields including approvedBy / approvedAt / rejectedBy / rejectedAt / rejectionReason when status is approved/rejected, and the action UI when status is open and `canApprove` is true), and a reason field (required for Reject, optional for Approve, enforced client-side by disabling the Reject button when `reason.trim().length === 0`). Mutations call the existing backend endpoints via `fetch('/api/backend/admin/automation/suggestions/:id/{approve|reject}', { method: 'POST', headers: { Accept, Content-Type, Authorization: Bearer <token>, X-Organization-Id: <orgId> } })` and use `router.refresh()` to re-render the server component after success. Optimistic local removal via `setRemovedIds` keeps the UI responsive while the refresh runs."
    - "Deleted `apps/cockpit-next/app/(app)/automation/suggestions/suggestions-stub.tsx` (the prior placeholder, 112 lines). The file was created by commit `2d258e1` per the plan-doc §Phase C preview as a deliberate 'no data yet' stub. The new `suggestions-client.tsx` is its real-data replacement; keeping the stub would create a dead code path."
    - "Updated `docs/automation/implementation-plan.md` §2 Status snapshot: added a C-3b+C-4 row, flipped the C-3 row from 'blocked on C-3a' to 'C-3a done (commit 5507df6), C-3b+C-4 done (this slice)'."
  - filesRead:
    - "`docs/automation/semi-automated-operations-layer.md` lines 1147-1180 (Cockpit UI Proposal for the Suggestions page) and lines 1336-1340 (filter UX)"
    - "`docs/automation/implementation-plan.md` §Phase C lines 95-105 (C-3, C-4, C-5 slice descriptions)"
    - "`docs/agent-team/mspr_logbook/2026-06-08-phase-c-read-suggestions.md` (style reference + the open-questions section that the C-3b slice does not need to resolve)"
    - "`docs/DECISIONS.md` ADR-0029 (just-accepted, the back-promotion of the C-3a read endpoints)"
    - "`apps/cockpit-next/app/(app)/dashboard/page.tsx` (full read; the existing server-component + dashboard-card pattern)"
    - "`apps/cockpit-next/app/(app)/alerts/page.tsx` (full read; the existing server-component + client-component for actions pattern)"
    - "`apps/cockpit-next/app/(app)/alerts/alerts-table-client.tsx` (full read; the existing client component for actions via `fetch('/api/alerts/...')`)"
    - "`apps/cockpit-next/app/(app)/settings/team/registered-member-approval-form.tsx` (full read; the existing client component for actions via `fetch('/api/backend/...')` with auth headers)"
    - "`apps/cockpit-next/app/api/backend/[...path]/route.ts` (full read; the proxy that forwards headers and handles 503 fallback)"
    - "`apps/cockpit-next/lib/supabase/server.ts` (full read; the server-side Supabase client with cookie auth)"
    - "`apps/cockpit-next/lib/supabase/queries/dashboard.ts` (full read; the existing query helper pattern + the `DashboardSnapshot` type)"
    - "`apps/cockpit-next/lib/supabase/queries/inventory.ts` (head 40; the query helper with filter pattern)"
    - "`apps/cockpit-next/lib/supabase/queries/workspaces.ts` (full read; the `WorkspaceSummariesResult` shape with `organizationId` and `currentRole`)"
    - "`apps/cockpit-next/app/providers/auth-provider.tsx` (full read; the `useAuth` context shape)"
    - "`apps/cockpit-next/app/components/page-scaffold.tsx` (full read; the page wrapper used by all routes)"
    - "`apps/cockpit-next/app/components/ui/button.tsx` (full read; the Button component with `loading`, `variant`, `size` props)"
    - "`apps/cockpit-next/app/components/ui/badge.tsx` (head 20; the Badge component for status pills)"
  - filesChanged:
    - "new: `apps/cockpit-next/lib/supabase/queries/automation-suggestions.ts` (~150 lines: types, listAutomationSuggestions, getAutomationSuggestion, countOpenAutomationSuggestions, status + type constant exports)"
    - "modified: `apps/cockpit-next/lib/supabase/queries/dashboard.ts` (+~10 lines: import the new helper, add `openSuggestions` to the snapshot type, wire the count, gate on org id)"
    - "modified: `apps/cockpit-next/app/(app)/dashboard/page.tsx` (+~25 lines: the new 'Automation-Vorschläge' card with German copy + status badge + link)"
    - "new: `apps/cockpit-next/app/(app)/automation/suggestions/suggestions-client.tsx` (~280 lines: list cards, filter bar, detail modal, role-aware Approve/Reject actions, reason field with required-on-reject, optimistic local removal + `router.refresh()`)"
    - "modified: `apps/cockpit-next/app/(app)/automation/suggestions/page.tsx` (rewritten: server-component + RLS-scoped read + role gate + AccessDenied/ErrorState/EmptyState handling, imports `SuggestionsClient` from the new client file)"
    - "deleted: `apps/cockpit-next/app/(app)/automation/suggestions/suggestions-stub.tsx` (the prior 112-line placeholder; superseded by the new client component)"
    - "modified: `docs/automation/implementation-plan.md` (§2 Status snapshot: C-3b+C-4 row added, C-3 row updated)"
  - commandsRun:
    - "`cd apps/cockpit-next && npx tsc --noEmit` (clean after the import-path fix from `../../../lib/...` to `../../../../lib/...`)"
    - "`npx tsc --noEmit -p tsconfig.json` (backend tsc clean; the new dashboard query helper does not affect the backend types)"
    - "`cd apps/cockpit-next && npx next build` (clean; the new `/automation/suggestions` route is listed at 2.94 kB, server-rendered dynamic; the dashboard rebuilt at 774 B; 0 errors, 0 warnings)"
    - "`npx vitest run` (485/485 cases green across 60 test files; 0 regressions; 3.1s)"
    - "`npx prisma validate` (clean; the Cockpit slice does not touch the schema; the helper is query-only)"
  - validationResults:
    - "`tsc` (Cockpit): clean"
    - "`tsc` (backend): clean"
    - "`next build` (Cockpit): clean; new `/automation/suggestions` route at 2.94 kB, dynamic server-rendered"
    - "`vitest` (backend): 485/485 green"
    - "`prisma validate`: clean"
    - "no `.env*` read"
    - "no service-role credentials introduced (the Cockpit's Supabase client uses the user's session JWT, which is RLS-scoped per the B-2 policies; the backend proxy forwards the user's JWT, no service-role impersonation)"
    - "no `InventoryMovement` or `InventoryStockSnapshot` writes (C-3b+C-4 are read + UI actions; the actual mutations go through the existing C-1 endpoints, which do not write to `InventoryMovement` / `InventoryStockSnapshot` per the spec)"
- **Review**:
  - status: pass
  - risks:
    - "**The Cockpit read path does NOT exercise the C-3a backend read endpoints.** It uses Supabase-direct via RLS. This is the right pattern (matches the dashboard, avoids unnecessary server-side `fetch` proxying) but it means the C-3a endpoints are not exercised by the Cockpit. The 6 new vitest cases in `tests/automation-suggestion.routes.test.ts` cover the C-3a endpoints (in-memory stub, no real DB), and the B-2 RLS policy `automation_suggestion_org_member_select` is the runtime org-scope gate. A follow-up ADR-0028.1 (per ADR-0028 Open Question §2) could add a Supabase-snapshot test that exercises both the RLS policy and the C-3a endpoints against the same data; not in scope for this slice."
    - "**The Cockpit read path uses Supabase RLS, not the Fastify role gate.** The spec text says 'Auth: Required (staff+).' The Fastify `suggestionReadRoles = ['admin', 'shift_lead', 'staff']` enforces this at the API layer. The Cockpit read path enforces it at the role layer (the `AccessDenied` for non-staff+ in `page.tsx` and the `listWorkspaceSummariesForCurrentUser().currentRole` check). Both layers are in place; the Cockpit's role gate is the primary boundary for the Cockpit session, the Fastify role gate is the primary boundary for any other consumer of the C-3a endpoints. The defense-in-depth holds."
    - "**The page is `export const dynamic = 'force-dynamic'`.** This means the page is server-rendered on every request, no static optimization. The dashboard and alerts pages use the same pattern. The reason: the suggestions list depends on the actor's session (RLS-scoped) and the search params. A static page would require a different auth model. The pattern matches the rest of the Cockpit; no change recommended."
    - "**The reason field is enforced client-side (Reject disabled when reason is empty) AND server-side (the C-1 reject endpoint returns 422 for empty reason).** A user with browser DevTools could enable the Reject button via CSS and submit with an empty reason; the server-side 422 would catch it. The client-side guard is UX; the server-side guard is the audit-trail guarantee."
    - "**The 'Open suggestions' count on the dashboard could be slow for very large `AutomationSuggestion` tables.** The B-1 index `AutomationSuggestion_organizationId_status_createdAt_idx` covers the count query (the `head: true` count with `eq('organizationId')` + `eq('status', 'open')` is satisfied by the index). For very large tables, the count is still O(matches) in the index, which is fast. No performance risk at the spec'd scale; a follow-up could add a denormalized counter on `AutomationRule` if the dashboard latency becomes a concern."
    - "**The mobile swipe-left action sheet (per the spec) is out of scope for C-3b.** The spec text 'Mobile UX: Full-Width Cards, Swipe-Left für Approve/Reject, Bottom-Sheet für Detail' is not implemented. The shipped slice uses the existing `state-panel` modal pattern (the same one used by the alerts page) for the detail. A future UX slice can add the swipe-left gesture using a small library or hand-rolled pointer events; not in scope for the C-3b+C-4 functional delivery."
    - "**The plan-doc §Phase C C-5 (bar-refill banner) is NOT in this slice.** The user explicitly scoped C-3b + C-4; C-5 is a separate slice. The plan-doc C-5 line ('Bar refill page: surface a refill suggestion if the low-stock rule matched') would integrate the new `GET /admin/automation/suggestions?type=refill&status=open` with the bar-refill page; the API surface it needs is complete."
  - scorecard:
    - outcomeQuality: 5 (the Cockpit stub is replaced with a real-data list + filter + detail + actions; the dashboard now has an 'Automation-Vorschläge' card with a live count; both are German-localized to match the existing Cockpit voice; the design uses the existing CSS classes; the build is clean)
    - scopeDiscipline: 5 (no schema change, no RLS change, no new migration, no new endpoint, no new test infrastructure; the C-3b+C-4 slice is the minimum needed to make the existing backend API surface usable from the Cockpit; C-5 is explicitly out of scope)
    - safety: 5 (Supabase-direct reads are RLS-scoped per the B-2 policies; backend mutations go through the existing `/api/backend/...` proxy which forwards the user's JWT; no service-role impersonation; no PII in the new copy; the reason field is enforced both client-side and server-side)
    - evidenceQuality: 5 (Cockpit tsc clean, backend tsc clean, `next build` clean (2.94 kB new route, 0 errors, 0 warnings), 485/485 backend vitest cases green, prisma validate clean; the build output confirms the new route is in the route table; the dashboard count is exercised by the `countOpenAutomationSuggestions` query; the read endpoints are exercised by `listAutomationSuggestions`; the live browser interaction (Approve/Reject buttons, modal close, router refresh) is the one thing not verified by automation, and is honestly flagged in the residual risk section)
    - sideEffects: 5 (1 new query helper; 1 dashboard query extension; 1 dashboard card added; 1 new client component; 1 server-component rewrite; 1 stub file deleted; 1 plan-doc row updated; 0 changes to backend code; 0 changes to tests; 0 changes to schema; 0 changes to RLS; 0 changes to other 60 test files)
  - nextGate: "**C-5: Phase C bar-refill banner** (per the plan-doc §Phase C C-5). Integrate the new `GET /admin/automation/suggestions?type=refill&status=open` (or the Supabase-direct equivalent) into the bar-refill page; show a 'System suggests refill needed: N items' banner when the count is > 0; link to `/automation/suggestions?type=refill&status=open`. The backend API surface is already complete (C-3a list endpoint + C-1 read endpoint). **Or** push the 6 local commits to origin (`git push origin main`) and stop here for now. **Or** move to Phase D (offline queue, ADR-0024 not yet drafted) as a follow-up governance slice."
