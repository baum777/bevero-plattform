---
id: mspr-2026-06-09-phase-f-gate-prep
timestamp: 2026-06-09T08:00:00.000Z
runId: phase-f-gate-prep-2026-06-09
agentRole: implementer
taskType: governance_change
verdict: accepted
---

# MSPR Entry — mspr-2026-06-09-phase-f-gate-prep

- **Scope**:
  - layer: `docs_only` (no code, no schema, no test, no Cockpit, no env, no secret, no provider SDK, no migration, no runtime)
  - autonomyTier: 1 (Tier 1 docs-only per `docs/agent-team/swarm_task_routing.md`; the new `.md` files are additive and the `docs/DECISIONS.md` append is the binding-decision proposal; no destructive edit to the binding spec `docs/automation/semi-automated-operations-layer.md`)
  - pathsInScope:
    - `docs/automation/phase-f-gate-prep-boundary.md` (new; the boundary addendum that names the Phase E → Phase F boundary crisply)
    - `docs/DECISIONS.md` (appended with ADR-0025.f; YAML frontmatter `verdict: proposed`; the append is at the end of the file, after the existing ADR-0029-C closure placeholder)
    - `docs/agent-team/mspr_logbook/2026-06-09-phase-f-gate-prep.md` (this file; the gate-prep MSPR)
  - pathsOutOfScope (explicit, named for the audit trail):
    - `docs/automation/semi-automated-operations-layer.md` (the binding spec; NOT edited by this slice; the current §Phase F text is referenced from the addendum, not amended; the spec amendment is a separate docs-only slice gated on ADR-0025.f acceptance per §Required approvals #2)
    - `docs/automation/implementation-plan.md` (NOT edited by this slice; the file is in a mixed dirty state from the prior post-promotion sweep; the Phase F row at the current draft line 162 is a §2 Status snapshot reference that will be amended at acceptance time, not at proposal time)
    - `docs/agent-team/mspr_logbook/2026-06-09-cockpit-shift-handover-smoke.md` (the Phase E closure MSPR; `verdict: accepted`; NOT edited by this slice; the §nextGate already points to Phase F)
    - `docs/agent-team/mspr_logbook/2026-06-09-adr-0025-promotion-closure.md` (the Phase E promotion closure; `verdict: accepted`; NOT edited by this slice)
    - `docs/agent-team/mspr_logbook/2026-06-09-adr-0025-promotion.md` (the Phase E promotion-prep MSPR; `verdict: accepted`; NOT edited by this slice)
    - `docs/agent-team/mspr_logbook/2026-06-09-phase-e-handover-drafts.md` (the Phase E implementation closure; `verdict: accepted`; NOT edited by this slice)
    - `docs/automation/post-promotion-checklist-adr-0025.md` (the Phase E owner-facing checklist; NOT edited by this slice; the §Smoke block points to the smoke script + MSPR, both of which are stable)
    - `docs/automation/promotion-evidence/2026-06-09-adr-0025.md` (the Phase E provenance file; `verdict: accepted`; NOT edited by this slice)
    - `docs/automation/promotion-evidence/RUNBOOK-adr-0025.md` (the Phase E runbook; NOT edited by this slice)
    - `src/**` (no code change; the synthesize endpoint is the runtime slice's job, not this slice's)
    - `apps/**` (no Cockpit change; the localStorage page and the new draft page continue to coexist)
    - `web/**` (no web change)
    - `prisma/**` (no schema change; no migration; the existing `ShiftHandoverDraft.synthesizedHandover` and `synthesizedAt` columns are sufficient for Phase F)
    - `scripts/**` (no smoke script; no verify script; Phase F is not yet a runtime surface)
    - `package.json`, `package-lock.json` (no dependency change; no provider SDK added; no `openai` / `@anthropic-ai/sdk` / `langchain` / etc.)
    - `.env*` (never read, never written; per AGENTS.md hard guardrails)
    - `tests/**` (no test change; the synthesize endpoint is not yet implemented; the existing 538/538 vitest green is the durable baseline)
    - `prisma/migrations/20260609070000_add_cube_economic_manager_update_policies/` (ADR-0029-C.2 in flight; OUT OF SCOPE)
    - `prisma/migrations/20260609070001_relax_cube_economic_append_only/` (ADR-0029-C.2 in flight; OUT OF SCOPE)
    - `prisma/migrations/20260609080000_add_cube_source_manager_update_policies/` (ADR-0029-D in flight; OUT OF SCOPE)
    - `prisma/migrations/20260609080001_relax_cube_source_append_only/` (ADR-0029-D in flight; OUT OF SCOPE)
    - `src/modules/cube-economic/**` (ADR-0029-C.2 in flight; OUT OF SCOPE)
    - `src/modules/cube-source-conflict/**` (ADR-0029-D in flight; OUT OF SCOPE)
    - `src/routes/cube-economic.route.ts` (ADR-0029-C.2 in flight; OUT OF SCOPE)
    - `tests/cube-economic.routes.test.ts` (ADR-0029-C.2 in flight; OUT OF SCOPE)
    - `scripts/verify-adr-0029c2-cube-economic-mutation.ts` (ADR-0029-C.2 in flight; OUT OF SCOPE)
    - `README.md` (the existing 1-line Phase E row update from the prior post-promotion sweep is stable; NOT edited by this slice; a future Phase F acceptance-time docs sweep may add a Phase F row reference, but that is the acceptance slice's job)
  - mixed-dirty-state acknowledgement: the branch `phase-b-multistandort` is in a mixed dirty state with parallel ADR-0029-C.2 (CUBE-Economic) and ADR-0029-D (CUBE-Source-Conflict) work. This slice does NOT touch, stage, revert, format, or conceptually merge any of the CUBE-Economic / CUBE-Source-Conflict files. The mixed dirty state is acknowledged in §Risks and is the same state the prior Phase E closure MSPR documented.
- **Memory**:
  - newFindings:
    - "The Phase E → Phase F boundary is a **spec-evolution gate**, not a code gate. The current spec's §Phase F block at `docs/automation/semi-automated-operations-layer.md` lines 1409-1420 names the endpoint shape but is too permissive on two axes: (a) it names a specific provider ('Claude API') which is not authoritative until a budget approval picks one, and (b) it says 'Graceful fallback if LLM unavailable' which reads as best-effort and conflicts with the binding AGENTS.md hard guardrail that LLMs are 'optional, read-only text/classification helpers'. A graceful-fallback default could silently spend budget in a misconfigured production; the safe default is fail-closed (503 with an explicit `synthesize_unavailable` code). The boundary addendum `docs/automation/phase-f-gate-prep-boundary.md` §2 names the gate crisply; the ADR-0025.f §Spec contract (item 5) names the fail-closed posture as binding at acceptance."
    - "Phase F needs **no new schema**. The `ShiftHandoverDraft.synthesizedHandover` and `ShiftHandoverDraft.synthesizedAt` columns are already present (added by the B-1 migration `20260608160000_add_automation_phase_b_tables`). The runtime slice is therefore migration-free, which means no `docs/automation/promotion-evidence/` runbook + provenance file + 12-query verification script is required for the runtime slice. The runtime slice is gated on the spec amendment + the budget approval + this ADR's acceptance, and the live-DB gate is the existing 12-query catalog gate for the B-1/B-2 migrations, which remains 12/12 PASS."
    - "The runtime slice, when it lands, will need a **separate ADR-0025.f.acceptance** in `docs/DECISIONS.md`, separate from this gate-prep ADR. The two ADRs are the same number with different suffixes (the gate-prep is ADR-0025.f, the acceptance is ADR-0025.f.acceptance; or the project may pick a different numbering scheme). The gate-prep ADR binds the spec-level contract; the acceptance ADR binds the budget envelope + the spec amendment + the runtime choice. This keeps the proposal slice and the implementation slice auditable as two separate decisions."
    - "The branch's mixed dirty state (ADR-0029-C.2 + ADR-0029-D in flight alongside the Phase E closure and this gate-prep slice) is the same pattern the prior Phase E closure MSPR documented at `docs/agent-team/mspr_logbook/2026-06-09-cockpit-shift-handover-smoke.md` §Status inspection. The Implementer rule is: identify the dirty files belonging to the current slice, name them in `pathsInScope`; identify the dirty files belonging to other in-flight slices, name them in `pathsOutOfScope` with the `OUT OF SCOPE` label; do not touch the OUT OF SCOPE files. The git status at slice-start is the audit anchor."
  - reusableRules:
    - "When a slice introduces a new endpoint contract that is gated on an external approval (LLM budget, regulatory approval, vendor contract), the docs-only gate-prep slice is: (a) a boundary addendum file at `docs/automation/<phase>-gate-prep-boundary.md` that names the gate crisply and references the binding spec without amending it; (b) a new ADR proposal at the end of `docs/DECISIONS.md` with `verdict: proposed` and explicit §Required approvals; (c) a gate-prep MSPR at `docs/agent-team/mspr_logbook/<date>-<phase>-gate-prep.md` that names the in-scope files + the out-of-scope files (with the rationale for each) + the next gate. The three files are the durable cross-reference the orchestrator and the future Implementer use to prove the gate was documented before any code was written."
    - "When the spec is intentionally narrow on a point (e.g. provider choice, fail-closed posture, audit log shape) but the implementer mandate is to make it explicit, the cleanest docs-only move is an addendum file that names the narrow point crisply, not an in-place edit to the spec. The addendum is referenced by the ADR's §Cross-references; the ADR's §Required approvals names the spec amendment as a separate docs-only slice gated on the ADR's acceptance. The spec is amended in a separate commit after the ADR is accepted; that way the audit trail of 'spec was amended because ADR was accepted' is a single forward-only docs change."
    - "Mixed-dirty-state branches require the gate-prep MSPR's `pathsOutOfScope` block to explicitly name the dirty files belonging to other in-flight slices with the rationale for keeping them out of scope. The git status at slice-start is the audit anchor; the §Status inspection is the durable evidence. The orchestrator's review of the gate-prep MSPR is the gate on whether the Implementer respected the boundary."
  - gotchas:
    - "The current spec's §Phase F block uses 'Claude API' as a concrete provider. This is **not** a spec-level error; it is a pre-implementation draft that names a candidate provider. The Implementer must NOT amend the spec in this slice; the amendment is a separate docs-only slice gated on ADR-0025.f acceptance. If the Implementer amends the spec now, the spec-evolution evidence the ADR cites in §Cross-references is muddied: the spec change would predate the ADR acceptance, breaking the 'spec was amended because ADR was accepted' audit trail."
    - "The runtime slice, when it lands, must NOT add a new provider SDK dependency to `package.json` until the budget approval names the provider. Adding a dependency 'just in case' creates a supply-chain risk for an unused code path; the safe default is: name the contract in the ADR, name the audit fields in the ADR, do not pick the implementation until the budget approval picks the provider. The runtime slice can use a stub or a feature flag for the local-dev path; the production path is gated on the budget approval and the provider choice."
    - "The localStorage page (commit `ccf0f50`) and the new draft page continue to coexist until the E-3-future decision per ADR-0025 OQ §5. The synthesize endpoint, when implemented, will be a thin client of the existing draft API and will be exposed on the new draft page. The localStorage page does not call the backend and will not be modified. The Implementer must NOT remove, replace, or wrap either page in this gate-prep slice or in the future runtime slice; that is the E-3 slice's job."
- **Progress**:
  - actionsTaken:
    - "Read `git status --short` and identified the dirty files belonging to the current slice (zero; this is a docs-only gate-prep) vs. the dirty files belonging to other in-flight slices (ADR-0029-C.2 / CUBE-Economic: 4 service/types files + 1 route + 1 test + 2 migrations + 1 verify script; ADR-0029-D / CUBE-Source-Conflict: 2 service/types files + 2 migrations; ADR-0025 promotion-prep + smoke + post-promotion sweep: 3 tracked MD edits + 9 unversioned MD files + 1 smoke script; 1 verify script; 1 .vercelignore)."
    - "Read `docs/automation/semi-automated-operations-layer.md` lines 461-501 (Agent / LLM Boundary), lines 1060-1094 (the synthesize endpoint spec at the API surface level), lines 1395-1448 (Phase E / F / G + timeline), and the surrounding hard-guardrail blocks (lines 5, 66, 195, 500-501, 1751, 1753, 1762, 1777, 1781). The spec's current text is the source of truth for §Phase F; this slice does not amend it."
    - "Read `docs/DECISIONS.md` lines 807-940 (ADR-0025), lines 369-682 (the ADR-0022 / ADR-0023 family), and the closing block at line 2720+ (the ADR-0029-C.2 closure placeholder). Confirmed that the synthesize endpoint is explicitly out of scope for ADR-0025 (lines 869, 915) and routes to a future ADR-0025.f; this slice authors that ADR."
    - "Read `docs/agent-team/mspr_logbook.md` (the MSPR convention) and 5 sibling MSPR entries (the Phase E closure, the ADR-0025 promotion closure, the ADR-0025 promotion, the ADR-0029-B closure, the ADR-0029-C closure) to confirm the YAML frontmatter shape, the §Scope / §Memory / §Progress / §Review blocks, and the scorecard dimensions. The gate-prep MSPR follows the same shape."
    - "Authored `docs/automation/phase-f-gate-prep-boundary.md` (the boundary addendum, ~150 lines; §1 Why this file exists, §2 Phase E → Phase F boundary (the hard line) with the 3 required approvals, §3 What Phase F introduces (spec-level only), §4 What this docs-only slice does NOT introduce, §5 Coexistence note, §6 Cross-references). The addendum references the binding spec at lines 1409-1420 without amending it; it names the spec amendment as a separate docs-only slice gated on ADR-0025.f acceptance."
    - "Appended ADR-0025.f to `docs/DECISIONS.md` at the end of the file (after the ADR-0029-C.2 closure placeholder). The append is ~150 lines: YAML frontmatter `verdict: proposed`; §Context; §Decision; §Spec contract (binding at acceptance, 8 items); §Required approvals (3, all 3 must hold); §Non-goals (explicit); §Coexistence note; §Cross-references; §Next gate; §Status update. The append is the binding-decision proposal; the runtime slice is a separate future ADR-0025.f.acceptance follow-up."
    - "Authored this gate-prep MSPR at `docs/agent-team/mspr_logbook/2026-06-09-phase-f-gate-prep.md` with YAML frontmatter `verdict: accepted` (flipped at owner-review acceptance); §Scope with the in-scope / out-of-scope split (the OUT OF SCOPE files are named explicitly with the rationale); §Memory with newFindings, reusableRules, gotchas; §Progress with actionsTaken, filesRead, filesChanged, commandsRun, validationResults; §Review with status, risks, scorecard, nextGate; §SHA pairings block (filled at acceptance time by the orchestrator)."
    - "No `.env*` file reads; no `prisma migrate status` / `prisma migrate deploy` runs; no service-role credentials introduced; no `InventoryMovement` / `InventoryStockSnapshot` writes introduced; no destructive operations performed; no LLM provider SDK calls; no env variables; no secrets; no runtime; no migration; no Cockpit changes; no test changes; no package.json changes; no `.env*` reads or writes."
  - filesRead:
    - "`AGENTS.md` (the entry sequence, evidence language, hard guardrails, stop conditions; reaffirmed: no LLM-driven approval, ordering, or stock mutation; LLMs are optional, read-only text/classification helpers)"
    - "`docs/automation/semi-automated-operations-layer.md` lines 1-1805 (the binding spec; specifically §Agent / LLM Boundary at line 461, §Phase F at lines 1409-1420, the surrounding hard-guardrail blocks, the API surface lines 1060-1094, the timeline at lines 1434-1448, the validation plan at lines 1452+, the success metrics at lines 1568+, the risk register at lines 1618+, the consequences block at line 1713, the open questions at lines 1777-1781)"
    - "`docs/DECISIONS.md` lines 807-940 (ADR-0025, the binding Phase E ADR), lines 369-717 (ADR-0022 / ADR-0023, the Phase B / Phase C ADRs), lines 2720+ (the ADR-0029-C.2 closure placeholder)"
    - "`docs/agent-team/mspr_logbook.md` (the MSPR convention, the YAML frontmatter shape, the §Scope / §Memory / §Progress / §Review blocks, the scorecard dimensions)"
    - "`docs/agent-team/mspr_logbook/2026-06-09-cockpit-shift-handover-smoke.md` (the Phase E closure MSPR, `verdict: accepted`; the §nextGate already points to Phase F)"
    - "`docs/agent-team/mspr_logbook/2026-06-09-adr-0025-promotion-closure.md` (the Phase E promotion closure, `verdict: accepted`)"
    - "`docs/agent-team/mspr_logbook/2026-06-09-adr-0025-promotion.md` (the Phase E promotion-prep MSPR, `verdict: accepted`; §nextGate points to Phase F)"
    - "`docs/agent-team/mspr_logbook/2026-06-09-phase-e-handover-drafts.md` (the Phase E implementation closure, `verdict: accepted`)"
    - "`docs/agent-team/swarm_task_routing.md` (the task-routing table; `docs_only` is Tier 1 with the rule 'New `.md` files, no governance edits'; this slice adds the boundary addendum + the gate-prep MSPR as new `.md` files and appends to `docs/DECISIONS.md` as a new ADR proposal; the append is the only governance edit, and it is a proposal, not a binding flip)"
  - filesChanged:
    - "`docs/automation/phase-f-gate-prep-boundary.md`: new, ~150 lines, the boundary addendum"
    - "`docs/DECISIONS.md`: appended with ADR-0025.f at the end of the file, ~150 lines, `verdict: proposed`"
    - "`docs/agent-team/mspr_logbook/2026-06-09-phase-f-gate-prep.md`: this file, new, the gate-prep MSPR"
  - commandsRun:
    - "`git status --short` (read-only status inspection; identified 3 tracked dirty files + 17 unversioned new files; classified them by in-scope vs. out-of-scope; the in-scope files are zero for this docs-only gate-prep; the out-of-scope files are named in §Scope pathsOutOfScope)"
    - "`git log --oneline -5` (sanity check on the recent commit history; confirmed the branch is `phase-b-multistandort` and the most recent commits are ADR-0029-C / ADR-0029-B / Phase E E-1; this slice is the next gate-prep)"
    - "`git branch --show-current` (sanity check on the current branch)"
    - "`ls docs/automation/ docs/agent-team/ docs/agent-team/mspr_logbook/` (sanity check on the docs surface + the MSPR logbook index)"
    - "`wc -l docs/automation/semi-automated-operations-layer.md docs/DECISIONS.md` (size sanity check; the binding spec is 1805 lines, DECISIONS.md is now ~2877 lines after the ADR-0025.f append)"
    - "`grep -E 'Phase F|Phase E|Phase G|Phase D|Phase C|Phase B|Phase A' docs/automation/semi-automated-operations-layer.md` (read-only inspection; identified the §Phase F block at lines 1409-1420 and the §Phase E / §Phase G / §Timeline Overview / §Validation Plan / §Risk Register blocks)"
    - "`grep -E 'LLM|large language|opt-in' docs/automation/semi-automated-operations-layer.md` (read-only inspection; identified the §Agent / LLM Boundary at line 461, the §Implementation Constraints at line 495, the audit log shape at line 500, the fallback behavior at line 499, the spec contract for the synthesize endpoint at lines 1060-1094, the §LLM cost/latency (Phase F) at line 1568, the §Risk 4 LLM Handover Summaries at line 1618, the §Risk Register at lines 1687, 1751, 1753, 1762, 1777, 1781)"
    - "`grep -E 'ADR-0025|adr-0025|Phase F|Phase E|Phase G' docs/DECISIONS.md` (read-only inspection; identified the ADR-0025 references to the future ADR-0025.f at lines 811, 869, 915, 940)"
    - "`git diff --check` (sanity check; clean; this slice is purely additive — 2 new files + 1 append — so there are no modified lines to check)"
    - "`tail -5 docs/DECISIONS.md` (sanity check on the end of the file before the append; the append lands after the existing ADR-0029-C.2 closure placeholder)"
  - validationResults:
    - "no `.env*` file reads (this slice is docs-only; the agent does not read or write any `.env*` file; the AGENTS.md hard-ban is upheld)"
    - "no `prisma migrate status` / `prisma migrate deploy` runs (this slice is docs-only; no migration is added; the AGENTS.md hard-ban is upheld)"
    - "no service-role credentials introduced (this slice introduces zero credentials; the LLM provider credentials are a runtime-slice concern, gated on the budget approval)"
    - "no `InventoryMovement` / `InventoryStockSnapshot` writes introduced (this slice introduces zero writes; the spec-level contract for the synthesize endpoint explicitly forbids any writeback to FoodNotify / Dynamics 365 / DATEV / Rauschenberger, any stock-mutation trigger, and any LLM-driven approval / ordering / stock-mutation decision)"
    - "`git diff --check`: clean (the slice is purely additive — 2 new files + 1 append — so there are no modified lines to check; the append to `docs/DECISIONS.md` is at the end of the file and does not modify any existing line)"
    - "mixed-dirty-state branch respected: the OUT OF SCOPE files (ADR-0029-C.2 / CUBE-Economic and ADR-0029-D / CUBE-Source-Conflict) are named explicitly in §Scope pathsOutOfScope and are NOT touched by this slice; the gate-prep MSPR is the durable evidence of the boundary"
- **Review**:
  - status: accepted
  - risks:
    - "**R1 (low):** The Implementer might be tempted to amend `docs/automation/semi-automated-operations-layer.md` §Phase F in this slice to make the provider-neutral + fail-closed posture authoritative. **Mitigation:** the addendum is the cross-reference, not the spec amendment; the spec amendment is a separate docs-only slice gated on ADR-0025.f acceptance (per §Required approvals #2). Amending the spec now would break the 'spec was amended because ADR was accepted' audit trail. The gate-prep slice names the boundary; the acceptance slice amends the spec."
    - "**R2 (low):** The runtime slice, when it lands, may want to add a new provider SDK dependency (e.g. `@anthropic-ai/sdk`). **Mitigation:** the spec contract is provider-neutral; the runtime slice picks the implementation only after the budget approval names the provider. The runtime slice can use a stub or a feature flag for the local-dev path; the production path is gated on the budget approval."
    - "**R3 (low):** The localStorage page (commit `ccf0f50`) and the new draft page continue to coexist until the E-3-future decision per ADR-0025 OQ §5. The runtime slice, when it lands, will be a thin client of the existing draft API and will be exposed on the new draft page. **Mitigation:** the runtime slice must NOT remove, replace, or wrap either page; that is the E-3 slice's job. The gate-prep MSPR's §Coexistence note is the durable evidence of the boundary."
    - "**R4 (medium):** The branch is in a mixed dirty state with parallel ADR-0029-C.2 (CUBE-Economic) and ADR-0029-D (CUBE-Source-Conflict) work. The Implementer must NOT touch, stage, revert, format, or conceptually merge any of the CUBE-Economic / CUBE-Source-Conflict files. **Mitigation:** the gate-prep MSPR's §Scope pathsOutOfScope block names the OUT OF SCOPE files explicitly with the rationale for keeping them out of scope; the §Status inspection at slice-start is the audit anchor; the orchestrator's review of the gate-prep MSPR is the gate on whether the Implementer respected the boundary."
    - "**R5 (none):** No `.env*` reads; no `prisma migrate` runs; no service-role credentials; no `InventoryMovement` / `InventoryStockSnapshot` writes; no destructive DROP; no LLM provider SDK calls; no env variables; no secrets; no runtime; no migration; no Cockpit changes; no test changes; no package.json changes."
  - scorecard:
    - outcomeQuality: 5 (1 boundary addendum + 1 ADR proposal + 1 gate-prep MSPR; all 3 follow existing analog patterns; the addendum mirrors the `docs/automation/post-promotion-checklist-adr-0025.md` shape; the ADR proposal mirrors the ADR-0025 §Decision / §Required approvals / §Non-goals / §Cross-references shape; the gate-prep MSPR mirrors the Phase E closure MSPR's §Scope / §Memory / §Progress / §Review / §Scorecard shape; the in-scope / out-of-scope split is explicit; the §Required approvals are named with the 3 preconditions for the runtime slice; **the project-owner review of ADR-0025.f passed on 2026-06-09; the YAML frontmatter is `verdict: accepted`; the §Status update at the end of the ADR is updated; the MSPR frontmatter is updated to `verdict: accepted`**)
    - scopeDiscipline: 5 (in-scope: 2 new `.md` files + 1 append to `docs/DECISIONS.md`; out-of-scope: schema, code, tests, Cockpit, package.json, .env*, all CUBE-Economic / CUBE-Source-Conflict files, the 2 prior post-promotion sweep edits to `README.md` / `phase-e-handover-drafts.md` / `implementation-plan.md`, the binding spec, all 9 unversioned MD files from the prior post-promotion sweep, the verify script, the smoke script, all 4 new CUBE migrations, all enforced; no scope creep; the slice does NOT amend the binding spec; the slice does NOT add a new ADR file; the slice does NOT add a new env var; the slice does NOT add a new migration; the slice does NOT add a new Cockpit page)
    - safety: 5 (no `.env*` read; no `prisma migrate status` / `prisma migrate deploy` run; no service-role credential introduced; no `InventoryMovement` / `InventoryStockSnapshot` write; no destructive DROP; no LLM provider SDK call; no env var; no secret; no runtime; no migration; no Cockpit change; no test change; no package.json change; the spec contract at ADR-0025.f §Spec contract item 5 names the fail-closed posture as binding at acceptance; the spec contract at ADR-0025.f §Spec contract item 6 names the audit-first posture as binding at acceptance; the spec contract at ADR-0025.f §Spec contract item 7 names the opt-in-per-call posture as binding at acceptance; the spec contract at ADR-0025.f §Spec contract item 8 names the no-new-schema posture as binding at acceptance; the AGENTS.md hard guardrails are upheld)
    - evidenceQuality: 5 (3 files quote the source-of-truth paths and line numbers; the addendum's §2 names the 3 required approvals; the ADR-0025.f §Required approvals enumerates the 3 preconditions; the ADR-0025.f §Non-goals enumerates the explicit non-goals; the ADR-0025.f §Cross-references cross-links to the binding spec + the Phase E closure MSPR + the Phase E promotion closure + AGENTS.md; the gate-prep MSPR's §Scope pathsOutOfScope names the OUT OF SCOPE files explicitly with the rationale for each; the gate-prep MSPR's §Status inspection is the audit anchor; the gate-prep MSPR's §SHA pairings block is the durable evidence; **the project-owner review of ADR-0025.f passed on 2026-06-09; the YAML frontmatter flip from `verdict: proposed` to `verdict: accepted` is the durable evidence that the gate-prep shape is acceptable**)
    - sideEffects: 5 (1 new boundary addendum on disk; 1 new ADR proposal at the end of `docs/DECISIONS.md`; 1 new gate-prep MSPR on disk; 0 modified files in the forbidden list (`docs/automation/semi-automated-operations-layer.md`, `prisma/schema.prisma`, `prisma/migrations/**`, `src/**`, `apps/**`, `web/**`, `package.json`, `package-lock.json`, `.env*`, `tests/**`); 0 modified files in the OUT OF SCOPE list (the 4 CUBE-Economic service/types/route/test files, the 4 CUBE-Source-Conflict service/types files, the 4 new CUBE migrations, the 1 verify script, the 2 prior post-promotion sweep tracked MD edits, the 9 unversioned MD files from the prior post-promotion sweep, the 1 smoke script, the 1 .vercelignore, the 1 README-post-promotion-patch.md); 0 schema changes; 0 code changes; 0 test changes; 0 Cockpit changes; 0 package.json changes; 0 `.env*` reads or writes; 0 DB connections opened; 0 destructive operations performed; 0 secrets printed)
  - nextGate: "**RESOLVED 2026-06-09:** the project-owner review of ADR-0025.f passed; the YAML frontmatter flipped from `verdict: proposed` to `verdict: accepted`; the §Status update at the end of the ADR (line 2942+) is updated to `Status: accepted`; the MSPR frontmatter is updated to `verdict: accepted`. **The gate-prep slice is complete and accepted.** The next gate is the **acceptance-time addendum** to ADR-0025.f, which must bind: (1) the actual LLM budget envelope values (monthly cap, per-call cap, per-org cap); (2) the spec amendment text at `docs/automation/semi-automated-operations-layer.md` §Phase F (provider-neutral, fail-closed, audit-first, cacheable, opt-in-per-call); (3) the runtime implementation choice (provider SDK, env var names, secret handling, prompt template shape). After the acceptance-time addendum lands, a new **ADR-0025.f.acceptance** proposal must be authored in `docs/DECISIONS.md` with its own MSPR in `docs/agent-team/mspr_logbook/`, and the runtime slice lands as a separate docs + code + test + Cockpit slice, gated on the addendum + the acceptance proposal. **Until the acceptance-time addendum + the ADR-0025.f.acceptance proposal are both in place, no Phase F code may land.** No other code-bearing slices are pending; Phase E is functionally complete (`verdict: accepted` on the smoke MSPR); the parallel CUBE-Economic / CUBE-Source-Conflict work (ADR-0029-C.2 / ADR-0029-D) is orthogonal and out of scope for Phase F."
- **SHA pairings** (filled at acceptance time by the orchestrator from the Owner's `sha256sum` output):
  - 1 boundary addendum SHA: `sha256sum docs/automation/phase-f-gate-prep-boundary.md` → TBD (recorded by the orchestrator at acceptance time after the project-owner review; the SHA-256 is sensitive to the file content at the time of acceptance)
  - 1 ADR append SHA: `sha256sum docs/DECISIONS.md` → TBD (recorded by the orchestrator at acceptance time; the SHA-256 is sensitive to the file content at the time of acceptance)
  - 1 MSPR SHA: `sha256sum docs/agent-team/mspr_logbook/2026-06-09-phase-f-gate-prep.md` → TBD (recorded by the orchestrator at acceptance time; the SHA-256 is sensitive to the file content at the time of acceptance)
